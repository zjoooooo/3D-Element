# M3 节奏 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 15 分钟一个骨架：五行潮汐轮转（敌群换色换构成）、吐息者/磐兽两个新行为模板与敌方弹道、精英与残章定向三选一、宝石分级与潮末金雨、克制倍率、自动施法、真正的结算屏。

**Architecture:** 全部延续 M1/M2 的结构——纯逻辑模块进 `src/run/`（Node 可测），渲染只读状态，RunManager 是唯一的接线处。潮汐是一个纯函数表（TideSchedule），敌人行为是 `behavior` 字节 + 每行为一张 settings 表，克制倍率下沉到 EnemySystem 内部（它知道自己的属性），门面 API 只多一个可选参数。

**Tech Stack:** 不变（Three.js / Vite / 原生 ESM / Node assert 校验脚本）。

## Global Constraints（每任务默认遵守，含 M1+M2 勘误规程）

- 修正层铁律：生效值 = settings 基值 × Modifiers；升级/潮汐/克制都不得写 settings
- run 冻结（选卡/结算）= 全停；**本里程碑起冷却也冻结**（M2 勘误：曾在 raw 上流逝）
- 固定 60Hz tick；热路径零分配；沙盒（无 `#run`）行为逐帧不变
- 所有数值进 settings 且同一任务内必须有读它的代码（勘误规程 ③）
- 接口块字段与方法不共名（M1 勘误：`slowed` 数组 vs `slow()` 方法）；落笔前对照真实文件核对锚点（规程 ①）
- 每个新公开方法至少一条会失败的断言（规程 ②）
- 敌方威胁一律红系（弹道、预告；spec §5.5）；玩家技能保持五行色
- cast 生命周期经 `abilities.onRetire` → `combat.release` → `enemies.releaseCast`（M2 定型，不得绕过）
- commit 用仓库自然句式，结尾 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

**本计划内的两个设计裁定**（spec 意图内的落地选择，用户可否决）：
1. **自动施法开关** = `Shift + 该槽按键`（鼠标槽用 Shift+点击 HUD 徽章），徽章上亮一个小蓝点；正式设置面板是 M5 的事。
2. **残章定向三选一兜底**：该系已满级满席无卡可发时，退化为普通回血跳过（skipHeal），并 toast 说明。

---

### Task 1: 账本清偿（延期断言批 + 三个已知缺陷 + 冻结冷却）

**Files:**
- Modify: `scripts/check-game.mjs`（追加断言块，插在 stress 块之前）
- Modify: `src/run/RunManager.js`（跨级里程碑修复）
- Modify: `src/run/UpgradePool.js`（跨级里程碑参数）
- Modify: `src/run/Loadout.js`（调试双席位去重）
- Modify: `src/core/App.js`（echo 覆盖窗口、冻结时冷却不流逝、`move()` JSDoc 补 `@param speedScale`）

**Interfaces:**
- Consumes: 现有 `Loadout / UpgradePool / Modifiers / RunManager` 全部公开方法（见各文件）
- Produces: `UpgradePool.draw(level, sinceLevel = level)`——跨级补抽：`[sinceLevel..level]` 区间内任何一级命中 `settings.upgrades.milestones` 即触发保新卡；`Loadout.reset()` 对配置数组去重（后出现的重复项置空席位）。

- [ ] **Step 1: 追加延期断言批（先失败的排最前）**

```js
// scripts/check-game.mjs 追加（放在 stress 块之前）：

/* ---- M2 ledger: deferred pins ---- */
{
  // Loadout: acquire refuses when full; upgrade refuses what isn't held.
  const loadout = new Loadout();
  settings.run.draftLoadout = false;
  loadout.reset();
  assert.equal(loadout.acquire('ice'), -1, 'loadout: full board refuses acquire');
  assert.ok(!loadout.upgrade('nosuch'), 'loadout: cannot upgrade an unheld skill');

  // Loadout: a duplicated id in the debug loadout must not seat twice.
  const dup = settings.run.loadout.slice();
  settings.run.loadout = ['ice', 'ice', 'thunder', 'meteor', 'beam', 'glacier'];
  loadout.reset();
  assert.equal(
    loadout.seats.filter((e) => e === 'ice').length, 1,
    'loadout: duplicate config ids seat once'
  );
  settings.run.loadout = dup;
  settings.run.draftLoadout = true;

  // RunManager.pendingLevels accumulates (+=, never =): two ticks that each
  // gain a level must leave two pending.
  const rng2 = createRng(11);
  const enemies2 = new EnemySystem(rng2);
  const pickups2 = new PickupSystem();
  const player2 = new PlayerState();
  const rm = new RunManager({
    enemies: enemies2, pickups: pickups2, player: player2, rng: rng2,
    combat: { tick: () => {}, release: () => -1 },
    targets: { register: () => {} },
    abilities: { active: [], onRetire: null }
  });
  rm.start();
  pickups2.xp = pickups2.xpNeed(1) + 0.5;
  rm.tick(1 / 60, { x: 0, z: 0 });
  pickups2.xp = pickups2.xpNeed(2) + 0.5;
  rm.tick(1 / 60, { x: 0, z: 0 });
  assert.equal(rm.pendingLevels, 2, 'run: pending levels accumulate across ticks');

  // UpgradePool: statistical kind coverage — 200 draws from a mid-run state
  // must produce every kind, and a milestone *span* (sinceLevel < milestone ≤
  // level) still guarantees a new-active card.
  const mods2 = new Modifiers();
  const pool = new UpgradePool(createRng(5), loadout, mods2);
  settings.run.draftLoadout = true;
  loadout.reset();
  const kinds = new Set();
  for (let n = 0; n < 200; n++) for (const card of pool.draw(2)) kinds.add(card.kind);
  assert.ok(
    kinds.has('upgrade') && kinds.has('new') && kinds.has('passive'),
    `pool: 200 draws must cover all kinds (saw ${[...kinds].join(',')})`
  );
  const spanHand = pool.draw(6, 4); // levels 4→6 jumped in one tick, milestone 5 inside
  assert.ok(
    spanHand.some((card) => card.kind === 'new'),
    'pool: a milestone crossed mid-span still guarantees a new active'
  );

  // Combat: the modifier amp reaches every kind, not just sweep. zoneTick is
  // the cheapest to pin headless: damage flows through targets.damage scaled.
  const seen = [];
  const combatAmp = new CombatSystem(
    { damage: (p, r, amt) => (seen.push(amt), 0), damageOnce: () => 0, slow: () => {} },
    { damageMult: (el) => (el === 'snare' ? 2 : 1) }
  );
  const snareCast = {
    element: 'snare', phase: 'impact', impactTime: 0.2, fadeTime: 0, u: 1,
    position: { x: 0, z: 0 }, origin: { x: 0, z: 0 },
    direction: { x: 1, z: 0 }, length: 1
  };
  combatAmp.tick(1 / 60, [snareCast]);
  const base = settings.combat.snare.dps * (1 / 60);
  assert.ok(
    seen.length && Math.abs(seen[0] - base * 2) < 1e-9,
    'combat: zoneTick damage rides the modifier amp'
  );
  console.log('ok  m2 ledger pins');
}
```

（`Loadout / RunManager / Modifiers / UpgradePool / CombatSystem` 的 import 已存在于脚本，缺哪个补哪个。）

- [ ] **Step 2: 运行确认失败**

Run: `npm run check:game`
Expected: FAIL —— 双席位断言（现有 `reset()` 不去重）与跨级 milestone 断言（`draw` 无第二参数）至少各失败其一。

- [ ] **Step 3: 实现四处修复**

`Loadout.reset()` 去重（替换 for 循环体）：

```js
  reset() {
    const configured = settings.run.loadout;
    this._levels = Object.create(null);
    for (let seat = 0; seat < 6; seat++) {
      const keep = settings.run.draftLoadout ? seat === 0 : true;
      const element = keep ? configured[seat] : null;
      // A debug loadout that repeats an id would deal duplicate cards later —
      // first seat wins, repeats sit out (M2 ledger).
      this.seats[seat] = element && !this.seats.slice(0, seat).includes(element) ? element : null;
      if (this.seats[seat]) this._levels[this.seats[seat]] = 1;
    }
  }
```

`UpgradePool.draw` 跨级签名（只改 `wantNew` 行）：

```js
  draw(level, sinceLevel = level) {
    // …候选生成不变…
    const wantNew =
      settings.upgrades.milestones.some((m) => m > sinceLevel - 1 && m <= level) &&
      candidates.some((c) => c.card.kind === 'new');
```

调用方 `App._onUpgradeChoice` 后续开手处把 `pool.draw(this.pickups.level)` 改为携带跨度：RunManager 暴露的 `pendingLevels` 消费循环中记录 `const sinceLevel = this.pickups.level - this.run.pendingLevels + 1;`，逐手传 `(levelForThisHand, sinceLevel === levelForThisHand ? levelForThisHand : sinceLevel)`——对照 App 真实消费代码核对变量名后落笔（规程 ①）。

echo 覆盖窗口（App `_echoAt` 处，M2 勘误）：第二次 proc 落在 0.15s 窗口内时**放弃本次 echo** 而不是覆盖首个待发：

```js
      if (this._echoAt !== null) {
        /* an echo is already queued — a second proc inside the snapback
           window would overwrite and eat the first; drop this one instead */
      } else {
        this._echoAt = ...; // 现有排队逻辑保持
      }
```

冻结时冷却不流逝（App 冷却衰减循环处，现读 `raw`）：

```js
    const frozen = this.runMode && this.upgradeUi.isOpen;
    if (!frozen) {
      for (const [element, remaining] of this.cooldowns) {
        if (remaining > 0) this.cooldowns.set(element, Math.max(0, remaining - raw));
      }
    }
```

`move()` JSDoc 补 `@param {number} [speedScale=1]` 一行。

- [ ] **Step 4: 双脚本全绿**

Run: `npm run check:game && npm run check`
Expected: 新增 `ok  m2 ledger pins`，其余全部保持绿。

- [ ] **Step 5: Commit**

```bash
git add scripts/check-game.mjs src/run/Loadout.js src/run/UpgradePool.js src/run/RunManager.js src/core/App.js
git commit -m "Pay down the M2 ledger: span milestones, dedup debug seats, guard the echo window, freeze cooldowns with the world"
```

---

### Task 2: 潮汐表 TideSchedule

**Files:**
- Create: `src/run/TideSchedule.js`
- Modify: `src/config/settings.js`（`run:` 块后新增 `tides:` 块）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Consumes: `createRng` 实例
- Produces:

```
export const WUXING = ['metal', 'wood', 'water', 'fire', 'earth']; // index = 敌人 element 字节
export const WUXING_LABEL = ['金', '木', '水', '火', '土'];
/** 五行相克：BEATS[i] 是 i 克的那一行。金克木 木克土 土克水 水克火 火克金 */
export const BEATS = [1, 4, 3, 0, 2];

class TideSchedule {
  constructor(rng)          // seeded shuffle of [0..4], one per run
  order: number[]           // the five tide elements in play order
  tideAt(elapsed): { index, element, progress, timeLeft, nextElement }
  rollElement(rng, elapsed): number   // tide element with `bias` probability, else uniform other
}
```

settings 新块：

```js
  /** The five 3-minute tides (spec §7). Order shuffles per run, seeded. */
  tides: {
    length: 180, // seconds per tide; 5 tides fill the 15-minute run
    bias: 0.7, // share of spawns that carry the tide's element
    goldRain: { count: 30, value: 5, radius: 6 }, // tide-end gem shower
    elitesPerTide: 2,
    eliteAt: [0.4, 0.75] // tide progress marks the elites spawn at
  },
```

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
import { TideSchedule, WUXING, BEATS } from '../src/run/TideSchedule.js';

/* ---- tides: a seeded permutation driving 3-minute windows ---- */
{
  const t1 = new TideSchedule(createRng(9));
  const t2 = new TideSchedule(createRng(9));
  assert.deepEqual(t1.order, t2.order, 'tides: same seed, same order');
  assert.deepEqual([...t1.order].sort(), [0, 1, 2, 3, 4], 'tides: all five, exactly once');

  const first = t1.tideAt(0);
  const second = t1.tideAt(settings.tides.length + 1);
  assert.equal(first.element, t1.order[0]);
  assert.equal(second.index, 1, 'tides: 181s sits in tide two');
  assert.equal(first.nextElement, t1.order[1], 'tides: next element previews');
  assert.ok(first.timeLeft > 179 && first.timeLeft <= 180);
  const last = t1.tideAt(899);
  assert.equal(last.index, 4, 'tides: the final second is still tide five');

  // Bias: over many rolls the tide element dominates at ~bias share.
  const rollRng = createRng(21);
  let tideHits = 0;
  for (let n = 0; n < 1000; n++) {
    if (t1.rollElement(rollRng, 0) === t1.order[0]) tideHits++;
  }
  assert.ok(tideHits > 600 && tideHits < 800, `tides: bias ≈70% (got ${tideHits}/1000)`);

  // The wuxing cycle: five entries, a permutation, and 金克木 as spot check.
  assert.equal(BEATS.length, 5);
  assert.deepEqual([...BEATS].sort(), [0, 1, 2, 3, 4]);
  assert.equal(BEATS[0], 1, 'wuxing: metal beats wood');
  assert.equal(WUXING.length, 5);
  console.log('ok  tide schedule');
}
```

- [ ] **Step 2: 确认失败** — module not found

- [ ] **Step 3: 实现**

```js
// src/run/TideSchedule.js
import { settings } from '../config/settings.js';

/**
 * The run's five 3-minute weather fronts (spec §7).
 *
 * A seeded Fisher-Yates deals the five elements into an order once per run;
 * everything else is arithmetic on elapsed seconds. Spawning asks
 * `rollElement` so 70% of a tide wears its colour and the rest keeps the
 * field mixed — a pure table, no state advances, which is what keeps it
 * assertable and (one day) lockstep-safe.
 */
export const WUXING = ['metal', 'wood', 'water', 'fire', 'earth'];
export const WUXING_LABEL = ['金', '木', '水', '火', '土'];
/** 相克 across the cycle: BEATS[i] is the element i overcomes. */
export const BEATS = [1, 4, 3, 0, 2];

export class TideSchedule {
  constructor(rng) {
    this.order = [0, 1, 2, 3, 4];
    for (let i = this.order.length - 1; i > 0; i--) {
      const j = (rng() * (i + 1)) | 0;
      const swap = this.order[i];
      this.order[i] = this.order[j];
      this.order[j] = swap;
    }
    this._out = { index: 0, element: 0, progress: 0, timeLeft: 0, nextElement: 0 };
  }

  /** Which tide `elapsed` seconds sits in. Returns a reused scratch object. */
  tideAt(elapsed) {
    const len = settings.tides.length;
    const index = Math.min(this.order.length - 1, (elapsed / len) | 0);
    const out = this._out;
    out.index = index;
    out.element = this.order[index];
    out.progress = Math.min(1, (elapsed - index * len) / len);
    out.timeLeft = Math.max(0, (index + 1) * len - elapsed);
    out.nextElement = this.order[Math.min(index + 1, this.order.length - 1)];
    return out;
  }

  /** An element for one spawn: the tide's own at `bias` odds, else any other. */
  rollElement(rng, elapsed) {
    const tide = this.tideAt(elapsed).element;
    if (rng() < settings.tides.bias) return tide;
    const other = (tide + 1 + ((rng() * 4) | 0)) % 5;
    return other;
  }
}
```

- [ ] **Step 4: settings 块落位 + 双脚本全绿** — `npm run check:game && npm run check`

- [ ] **Step 5: Commit**

```bash
git add src/run/TideSchedule.js src/config/settings.js scripts/check-game.mjs
git commit -m "Deal the five tides: a seeded order, a bias roll, the wuxing cycle"
```

---

### Task 3: 行为模板（吐息者/磐兽）+ 敌人五行字节 + 最近敌查询

**Files:**
- Modify: `src/config/settings.js`（`enemies:` 块补 ranged/tank 行与 mix 表）
- Modify: `src/run/EnemySystem.js`
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Consumes: 现有平铺数组结构
- Produces:

```
// EnemySystem 新增：
behavior: Uint8Array          // 0 swarm / 1 ranged / 2 tank
elite: Uint8Array             // 0 normal / 1 elite（Task 5 消费，本任务先落数组）
fireT: Float32Array           // ranged 开火冷却计时
spawnAt(x, z, minute, element = 0, behavior = 0, elite = 0): number
nearestTo(x, z): number       // 最近敌下标，空群返回 -1（autocast 消费）
// tick 行为分支：ranged 在 holdRange 外照常追、内则停走且不接触咬人；
// tank 用自己的 speed/contactDamage/mass（击退按 mass 折减已有公式自动生效）
// tick 返回值语义不变（最强单次接触伤害），tank 的 contactDamage 参与 max
```

settings `enemies:` 块补充（在 `swarm:` 行后）：

```js
    ranged: { speed: 2.4, radius: 0.5, contactDamage: 6, hpMult: 2, mass: 0.8, holdRange: 8, fireEvery: 2.4 },
    tank: { speed: 1.4, radius: 0.7, contactDamage: 20, hpMult: 6, mass: 4 },
    /** Behaviour mix by minute: shares ramp in as the run ages (sim's 70/20/10). */
    mix: { rangedFrom: 1.5, rangedShare: 0.2, tankFrom: 3, tankShare: 0.1 },
```

（`BEHAVIORS = ['swarm', 'ranged', 'tank']` 常量导出，`tick`/`damage` 内以 `settings.enemies[BEHAVIORS[this.behavior[i]]]` 查表——半径参与判定 reach 时改用**每敌自己的半径**，磐兽 0.7m 的大身板由此自动更好打，spec 所见即所判。）

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- behaviours: ranged holds range, tank shrugs knockback, nearest query ---- */
{
  const enemies = new EnemySystem(createRng(2));

  // Ranged: spawn inside holdRange → must not close further (and never bites).
  const r = enemies.spawnAt(4, 0, 0, 0, 1);
  for (let t = 0; t < 60; t++) enemies.tick(1 / 60, { x: 0, z: 0 }, 0);
  assert.ok(
    Math.hypot(enemies.x[r], enemies.z[r]) > 3.5,
    'behaviours: ranged holds its distance'
  );

  // Tank vs swarm knockback: same hit, the tank barely moves.
  enemies.clear();
  const s = enemies.spawnAt(2, 0, 0, 0, 0);
  const k = enemies.spawnAt(-2, 0, 0, 0, 2);
  enemies.damage({ x: 2, z: 0.01 }, 0.5, 1);
  enemies.damage({ x: -2, z: 0.01 }, 0.5, 1);
  assert.ok(
    Math.abs(enemies.kbZ[s]) > Math.abs(enemies.kbZ[k]) * 2,
    'behaviours: mass divides the shove'
  );

  // Element byte and elite byte survive the spawn signature.
  enemies.clear();
  const e = enemies.spawnAt(0, 0, 5, 3, 2, 1);
  assert.equal(enemies.element[e], 3);
  assert.equal(enemies.behavior[e], 2);
  assert.equal(enemies.elite[e], 1);

  // Nearest query.
  enemies.clear();
  assert.equal(enemies.nearestTo(0, 0), -1, 'behaviours: empty horde has no nearest');
  enemies.spawnAt(5, 0, 0);
  const near = enemies.spawnAt(1, 1, 0);
  assert.equal(enemies.nearestTo(0, 0), near, 'behaviours: nearest picks the closer body');
  console.log('ok  behaviours');
}
```

- [ ] **Step 2: 确认失败** — `spawnAt` 旧签名忽略新参数、`nearestTo` 不存在

- [ ] **Step 3: 实现**

EnemySystem 变更要点（完整落进文件）：

```js
export const BEHAVIORS = ['swarm', 'ranged', 'tank'];
```

构造器新增三数组：

```js
    this.behavior = new Uint8Array(cap);
    this.elite = new Uint8Array(cap);
    this.fireT = new Float32Array(cap);
```

`spawnAt(x, z, minute, element = 0, behavior = 0, elite = 0)`：

```js
    const kind = settings.enemies[BEHAVIORS[behavior]];
    this.hp[i] = settings.enemies.hpBase * (1 + settings.enemies.hpPerMinute * minute) * kind.hpMult;
    this.element[i] = element;
    this.behavior[i] = behavior;
    this.elite[i] = elite;
    this.fireT[i] = 0;
```

（elite 的 ×hpMult 二次放大在 Task 5 接线，spawn 只带标记。）

`tick` 循环内行为分支（替换现在写死 swarm 的 speed/contact 读法）：

```js
      const kind = settings.enemies[BEHAVIORS[this.behavior[i]]];
      const holding = this.behavior[i] === 1 && d < kind.holdRange;
      const v = holding ? 0 : kind.speed * (1 - this.slowed[i]);
      // …dx/dz 归一化乘 v 不变…
      if (!holding && d < kind.radius + 0.5) contact = Math.max(contact, kind.contactDamage);
```

击退按 mass：`damage`/`damageOnce` 内 `settings.enemies.swarm.mass` → `settings.enemies[BEHAVIORS[this.behavior[i]]].mass`；`reach` 内 `swarm.radius` → 每敌 `kind.radius`（三处 Targets 方法同改，circle-vs-circle 语义不变）。`_kill` 的 swap 数组列表补 `behavior / elite / fireT` 三项。

`nearestTo`：

```js
  /** Index of the closest live enemy to (x, z); -1 when the field is empty. */
  nearestTo(x, z) {
    let best = -1;
    let bestD = Infinity;
    for (let i = 0; i < this.count; i++) {
      const d = Math.hypot(this.x[i] - x, this.z[i] - z);
      if (d < bestD) {
        bestD = d;
        best = i;
      }
    }
    return best;
  }
```

- [ ] **Step 4: 双脚本全绿**（现有 stress/enemy 断言不得回归）

- [ ] **Step 5: Commit**

```bash
git add src/run/EnemySystem.js src/config/settings.js scripts/check-game.mjs
git commit -m "Teach the horde three gaits: swarmers rush, spitters hold range, tanks shrug"
```

---

### Task 4: 敌方弹道 EnemyProjectiles

**Files:**
- Create: `src/run/EnemyProjectiles.js`
- Modify: `src/run/EnemySystem.js`（ranged 开火钩子）
- Modify: `src/config/settings.js`（`enemies.projectile` 行）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Consumes: `EnemySystem.fireT/behavior` 数组
- Produces:

```
// settings.enemies 追加：
projectile: { speed: 7, radius: 0.3, damage: 12, life: 3 },

// EnemySystem 追加：
onFire: (x, z, dirX, dirZ) => void   // ranged 冷却归零时回调（tick 内发射）

class EnemyProjectiles {
  constructor()                       // cap 128 平铺数组 + 红色 Points（.points 挂 scene）
  count: number
  spawn(x, z, dirX, dirZ): void
  tick(step, player: {x, z}): number  // 命中玩家的总伤害（0.5m 玩家半径），命中即消
  sync(): void                        // 写入 Points 云（红色，size 0.5）
  clear(): void
}
```

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
import { EnemyProjectiles } from '../src/run/EnemyProjectiles.js';

/* ---- enemy projectiles: fly, hit, expire ---- */
{
  const shots = new EnemyProjectiles();
  shots.spawn(-5, 0, 1, 0); // flying +x toward the player at origin
  let dealt = 0;
  for (let t = 0; t < 120; t++) dealt += shots.tick(1 / 60, { x: 0, z: 0 });
  assert.equal(dealt, settings.enemies.projectile.damage, 'projectiles: a straight shot lands once');
  assert.equal(shots.count, 0, 'projectiles: a landed shot is gone');

  shots.spawn(0, 0, 0, 1); // flying away — must expire by lifetime
  for (let t = 0; t < 60 * settings.enemies.projectile.life + 5; t++) {
    shots.tick(1 / 60, { x: 99, z: 99 });
  }
  assert.equal(shots.count, 0, 'projectiles: lifetime reaps the strays');

  // The ranged enemy pulls the trigger through its hook while holding range.
  const enemies = new EnemySystem(createRng(4));
  let fired = 0;
  enemies.onFire = () => fired++;
  enemies.spawnAt(5, 0, 0, 0, 1);
  for (let t = 0; t < 60 * settings.enemies.ranged.fireEvery + 5; t++) {
    enemies.tick(1 / 60, { x: 0, z: 0 }, 0);
  }
  assert.ok(fired >= 1, 'projectiles: a holding spitter fires on its cadence');
  console.log('ok  enemy projectiles');
}
```

- [ ] **Step 2: 确认失败** — module not found

- [ ] **Step 3: 实现**

EnemySystem `tick` 的 ranged 分支追加（holding 判定处）：

```js
      if (this.behavior[i] === 1) {
        this.fireT[i] -= step;
        if (holding && this.fireT[i] <= 0) {
          this.fireT[i] = kind.fireEvery;
          this.onFire?.(this.x[i], this.z[i], (player.x - this.x[i]) / dist, (player.z - this.z[i]) / dist);
        }
      }
```

（构造器加 `this.onFire = null;`。）

```js
// src/run/EnemyProjectiles.js
import { BufferAttribute, BufferGeometry, Points, PointsMaterial } from 'three';
import { settings } from '../config/settings.js';

/**
 * The spitters' shots (spec §5): straight red bolts, dodgeable by the
 * spacebar's iframes downstream. Same flat arrays as everything else; the
 * Points cloud is red and slightly fat because threats read in red and
 * nothing else does (spec §5.5 敌我可读性).
 */
const CAP = 128;

export class EnemyProjectiles {
  constructor() {
    this.count = 0;
    this.x = new Float32Array(CAP);
    this.z = new Float32Array(CAP);
    this.vx = new Float32Array(CAP);
    this.vz = new Float32Array(CAP);
    this.age = new Float32Array(CAP);

    const geometry = new BufferGeometry();
    this._positions = new Float32Array(CAP * 3);
    geometry.setAttribute('position', new BufferAttribute(this._positions, 3));
    geometry.setDrawRange(0, 0);
    this.points = new Points(
      geometry,
      new PointsMaterial({ color: 0xff4433, size: 0.5, sizeAttenuation: true })
    );
    this.points.frustumCulled = false;
  }

  spawn(x, z, dirX, dirZ) {
    if (this.count >= CAP) return;
    const c = settings.enemies.projectile;
    const i = this.count++;
    this.x[i] = x;
    this.z[i] = z;
    this.vx[i] = dirX * c.speed;
    this.vz[i] = dirZ * c.speed;
    this.age[i] = 0;
  }

  /** Advance every shot; returns the damage that reached the player this tick. */
  tick(step, player) {
    const c = settings.enemies.projectile;
    let dealt = 0;
    for (let i = this.count - 1; i >= 0; i--) {
      this.x[i] += this.vx[i] * step;
      this.z[i] += this.vz[i] * step;
      if ((this.age[i] += step) > c.life) {
        this._remove(i);
        continue;
      }
      if (Math.hypot(this.x[i] - player.x, this.z[i] - player.z) < c.radius + 0.5) {
        dealt += c.damage;
        this._remove(i);
      }
    }
    return dealt;
  }

  _remove(i) {
    const last = --this.count;
    this.x[i] = this.x[last];
    this.z[i] = this.z[last];
    this.vx[i] = this.vx[last];
    this.vz[i] = this.vz[last];
    this.age[i] = this.age[last];
  }

  sync() {
    for (let i = 0; i < this.count; i++) {
      this._positions[i * 3] = this.x[i];
      this._positions[i * 3 + 1] = 0.9; // chest height — a shot, not a gem
      this._positions[i * 3 + 2] = this.z[i];
    }
    this.points.geometry.setDrawRange(0, this.count);
    this.points.geometry.attributes.position.needsUpdate = true;
  }

  clear() {
    this.count = 0;
  }
}
```

- [ ] **Step 4: 双脚本全绿**

- [ ] **Step 5: Commit**

```bash
git add src/run/EnemyProjectiles.js src/run/EnemySystem.js src/config/settings.js scripts/check-game.mjs
git commit -m "Let the spitters spit: red bolts the dash can slip through"
```

---

### Task 5: 精英（数值/渲染/掉落钩子）

**Files:**
- Modify: `src/config/settings.js`（`enemies.elites` 行）
- Modify: `src/run/EnemySystem.js`（elite 数值 + `onDeath` 带 elite 标记）
- Modify: `src/run/EnemyRenderer.js`（elite 放大+增亮）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Produces: `settings.enemies.elites = { hpMult: 40, scale: 1.6, gemValue: 15, damageMult: 1.5 }`；`spawnAt(..., elite = 1)` 时 HP 额外 ×`elites.hpMult`、接触伤害 ×`damageMult`；**`onDeath(x, z, element, elite)` 第四参**（现有两处消费者同步改签名）；渲染时 elite 实例 scale ×`elites.scale`、颜色向白提亮 0.35。

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- elites: forty lives and a marked corpse ---- */
{
  const enemies = new EnemySystem(createRng(6));
  const normal = enemies.spawnAt(0, 0, 2, 0, 0, 0);
  const boss = enemies.spawnAt(5, 5, 2, 0, 0, 1);
  assert.ok(
    Math.abs(enemies.hp[boss] / enemies.hp[normal] - settings.enemies.elites.hpMult) < 1e-6,
    'elites: hp multiplies by the elite factor'
  );
  let marked = null;
  enemies.onDeath = (x, z, element, elite) => (marked = elite);
  enemies.damage({ x: 0, z: 0 }, 0.5, 1e9); // kills the normal at origin only
  assert.equal(marked, 0, 'elites: a normal corpse reports elite=0');
  enemies.damage({ x: 5, z: 5 }, 0.5, 1e9);
  assert.equal(marked, 1, 'elites: an elite corpse says so');
  console.log('ok  elites');
}
```

- [ ] **Step 2: 确认失败** — `settings.enemies.elites` undefined / onDeath 三参

- [ ] **Step 3: 实现**

settings `enemies:` 块补：

```js
    elites: { hpMult: 40, scale: 1.6, gemValue: 15, damageMult: 1.5 },
```

EnemySystem：`spawnAt` HP 行尾乘 `(elite ? settings.enemies.elites.hpMult : 1)`；`tick` 接触伤害处乘 `(this.elite[i] ? settings.enemies.elites.damageMult : 1)`；`_kill` 的回调改 `this.onDeath?.(this.x[i], this.z[i], this.element[i], this.elite[i]);`。RunManager 现有 `onDeath = (x, z) => …` 接收处签名同步为 `(x, z, element, elite)`（掉落分级在 Task 6 接线，本任务只保证参数贯通）。

EnemyRenderer `render` 循环内：

```js
      const scale = pop * (enemies.elite[i] ? settings.enemies.elites.scale : 1);
      this._proxy.scale.set(scale, scale, scale);
      // …
      this._color.setHex(ELEMENT_TINTS[enemies.element[i]]);
      if (enemies.elite[i]) this._color.lerp(this._white, 0.35);
      this._color.lerp(this._white, enemies.flash[i]);
```

（`settings` 已 import ✓。）

- [ ] **Step 4: 双脚本全绿**

- [ ] **Step 5: Commit**

```bash
git add src/config/settings.js src/run/EnemySystem.js src/run/EnemyRenderer.js src/run/RunManager.js scripts/check-game.mjs
git commit -m "Raise the elites: forty lives, half again the size, a marked corpse"
```

---

### Task 6: 宝石分级 + 金珠雨 + 残章

**Files:**
- Modify: `src/run/PickupSystem.js`
- Modify: `src/config/settings.js`（`run.gemBlueMagnet` 一行）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Produces:

```
// PickupSystem 追加：
kind: Uint8Array                    // 0 green / 1 blue(全场磁吸) / 2 gold / 3 shard
dropAt(x, z, minute, kind = 0, value = auto): void
  // kind 1 → settings.enemies.elites.gemValue；kind 2 → settings.tides.goldRain.value
rainAt(x, z, rng): void             // goldRain.count 枚金珠撒在 radius 圆内
dropShard(x, z, element): void      // kind 3，value 存 element 下标
onShard: (element) => void          // 拾取残章时回调（不产 xp）
// tick：kind 1 的磁吸半径为 Infinity；kind 3 拾取走 onShard
// sync：按 kind 分色 —— Points 材质换 vertexColors，绿/蓝/金/白四色写进 color attribute
```

settings `run:` 块补一行 `gemBlueMagnet: 999, // elite gems fly from anywhere（spec 全场吸附）`。

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- gem tiers: blue flies far, gold rains, shards call back ---- */
{
  const pickups = new PickupSystem();

  pickups.dropAt(30, 0, 0, 1); // a blue gem far outside the normal magnet
  for (let t = 0; t < 60 * 6; t++) pickups.tick(1 / 60, { x: 0, z: 0 });
  assert.equal(pickups.count, 0, 'gems: blue magnets from across the arena');
  assert.ok(
    Math.abs(pickups.xp - settings.enemies.elites.gemValue) < 1e-6,
    'gems: blue carries the elite value'
  );

  pickups.clear();
  pickups.rainAt(0, 0, createRng(8));
  assert.equal(pickups.count, settings.tides.goldRain.count, 'gems: the rain drops its count');
  let total = 0;
  for (let i = 0; i < pickups.count; i++) total += pickups.value[i];
  assert.ok(
    Math.abs(total - settings.tides.goldRain.count * settings.tides.goldRain.value) < 1e-6,
    'gems: every raindrop is a gold value'
  );

  pickups.clear();
  let shardElement = null;
  pickups.onShard = (element) => (shardElement = element);
  pickups.dropShard(0.3, 0, 4);
  pickups.tick(1 / 60, { x: 0, z: 0 });
  assert.equal(shardElement, 4, 'gems: a shard reports its element, not xp');
  assert.equal(pickups.xp, 0, 'gems: shards carry no xp');
  console.log('ok  gem tiers');
}
```

- [ ] **Step 2: 确认失败** — 旧 `dropAt` 无 kind 参

- [ ] **Step 3: 实现**

PickupSystem 全量变更：构造器加 `this.kind = new Uint8Array(CAP); this.onShard = null;` 与 color attribute（`Float32Array(CAP*3)`、材质 `vertexColors: true` 去掉固定 color）；

```js
  dropAt(x, z, minute, kind = 0, value = null) {
    if (this.count >= CAP) return;
    const i = this.count++;
    this.x[i] = x;
    this.z[i] = z;
    this.kind[i] = kind;
    this.value[i] =
      value ??
      (kind === 1
        ? settings.enemies.elites.gemValue
        : kind === 2
          ? settings.tides.goldRain.value
          : settings.run.gemBase * (1 + settings.run.gemPerMinute * minute));
  }

  rainAt(x, z, rng) {
    const rain = settings.tides.goldRain;
    for (let n = 0; n < rain.count; n++) {
      const angle = rng() * Math.PI * 2;
      const r = Math.sqrt(rng()) * rain.radius;
      this.dropAt(x + Math.cos(angle) * r, z + Math.sin(angle) * r, 0, 2);
    }
  }

  dropShard(x, z, element) {
    this.dropAt(x, z, 0, 3, element);
  }
```

`tick` 内：磁吸半径 `const magnet = this.kind[i] === 1 ? settings.run.gemBlueMagnet : settings.run.magnetRadius;`（移进循环）；拾取分支：

```js
      if (d < 0.5) {
        if (this.kind[i] === 3) this.onShard?.(this.value[i] | 0);
        else this.xp += this.value[i] * (this.mods ? this.mods.xpMult() : 1);
        // …swap-remove 补 kind 数组…
```

`sync` 写 color：绿 `0.62,0.91,0.42` / 蓝 `0.35,0.65,1` / 金 `1,0.85,0.3` / 残章白 `1,1,1`（查 kind 写三分量，`needsUpdate` 同 position）。`clear()` 归零不变。

- [ ] **Step 4: 双脚本全绿**（既有 pickups 断言不得回归）

- [ ] **Step 5: Commit**

```bash
git add src/run/PickupSystem.js src/config/settings.js scripts/check-game.mjs
git commit -m "Grade the gems: blue flies the field, gold falls in sheets, shards speak"
```

---

### Task 7: 克制倍率 + 分技能伤害统计

**Files:**
- Modify: `src/config/settings.js`（`combat.wuxingOf` 映射 + `combat.matchup` 倍率）
- Modify: `src/run/EnemySystem.js`（damage/damageOnce 可选攻击方行参）
- Modify: `src/run/Targets.js`（透传第 4/5 参）
- Modify: `src/run/CombatSystem.js`（传行参 + `damageDealt` 统计）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Produces:

```
// settings.combat 追加两行：
wuxingOf: { ice: 2, glacier: 2, thunder: 1, snare: 1, meteor: 3, fireball: 3, beam: 0 },
matchup: { advantage: 1.25, disadvantage: 0.8 }, // spec §1 克制/被克

// Targets/EnemySystem 签名扩展（尾参可选，假人无视）：
damage(point, radius, amount, wuxing = -1)
damageOnce(castId, point, radius, amount, wuxing = -1)
  // wuxing >= 0 时每敌结算：BEATS[wuxing]===敌行 → ×advantage；BEATS[敌行]===wuxing → ×disadvantage

// CombatSystem 追加：
damageDealt: Record<element, number>   // 本局各技能已折算总伤（结算屏 top3 用）
resetStats(): void
```

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- matchups: the cycle taxes and rewards through the facade ---- */
{
  const enemies = new EnemySystem(createRng(12));
  // metal(0) beats wood(1): a metal hit on a wood enemy lands ×1.25.
  const wood = enemies.spawnAt(0, 0, 0, 1);
  const before = enemies.hp[wood];
  enemies.damage({ x: 0, z: 0 }, 1, 10, 0);
  assert.ok(
    Math.abs(before - enemies.hp[wood] - 10 * settings.combat.matchup.advantage) < 1e-6,
    'matchup: advantage lands ×1.25'
  );
  // wood beats earth(4): an earth hit on a wood enemy is the disadvantaged one? No —
  // wood(1) beats earth(4), so earth attacking wood pays the tax.
  const before2 = enemies.hp[wood];
  enemies.damage({ x: 0, z: 0 }, 1, 10, 4);
  assert.ok(
    Math.abs(before2 - enemies.hp[wood] - 10 * settings.combat.matchup.disadvantage) < 1e-6,
    'matchup: disadvantage pays ×0.8'
  );
  // Neutral pairs pass through untouched.
  const before3 = enemies.hp[wood];
  enemies.damage({ x: 0, z: 0 }, 1, 10, 2); // water vs wood: water feeds wood in 相生 but no 克 — neutral here
  assert.ok(Math.abs(before3 - enemies.hp[wood] - 10) < 1e-6, 'matchup: neutral is ×1');

  // CombatSystem books what each element dealt.
  const combatStats = new CombatSystem(
    { damage: () => 1, damageOnce: () => 2, slow: () => {} },
    null
  );
  const sweep = {
    element: 'ice', phase: 'travel', u: 0.5, position: { x: 1, z: 0 },
    origin: { x: 0, z: 0 }, direction: { x: 1, z: 0 }, length: 4
  };
  combatStats.tick(1 / 60, [sweep]);
  assert.ok(combatStats.damageDealt.ice > 0, 'stats: the ledger books ice damage');
  combatStats.resetStats();
  assert.ok(!combatStats.damageDealt.ice, 'stats: reset wipes the ledger');
  console.log('ok  matchups & stats');
}
```

- [ ] **Step 2: 确认失败** — 第 4 参被忽略、`damageDealt` 不存在

- [ ] **Step 3: 实现**

EnemySystem `damage`/`damageOnce` 增尾参并在扣血前折算（`BEATS` 从 TideSchedule import）：

```js
      let dealt = amount;
      if (wuxing >= 0) {
        const target = this.element[i];
        if (BEATS[wuxing] === target) dealt *= settings.combat.matchup.advantage;
        else if (BEATS[target] === wuxing) dealt *= settings.combat.matchup.disadvantage;
      }
      this.onHit?.(this.x[i], this.z[i], dealt);
      if ((this.hp[i] -= dealt) <= 0) this._kill(i);
```

Targets 两方法透传尾参（`p.damage(point, radius, amount, wuxing)`；假人签名三参，多传自然忽略）。CombatSystem：每处 targets 调用尾接 `settings.combat.wuxingOf[ability.element] ?? -1`，并以返回的命中数记账：

```js
      const hits = this.targets.damageOnce(castId, this._p, c.width, amt, wux);
      if (hits) this.damageDealt[ability.element] = (this.damageDealt[ability.element] ?? 0) + amt * hits;
```

（四种 kind 的每处 damage/damageOnce 调用同型改写；构造器初始化 `this.damageDealt = Object.create(null);`，`resetStats()` 重建。统计记名义伤害 amt×hits——克制折算发生在敌人内部，账面用于排名，量级一致即可，注释说明。）

- [ ] **Step 4: 双脚本全绿**

- [ ] **Step 5: Commit**

```bash
git add src/config/settings.js src/run/EnemySystem.js src/run/Targets.js src/run/CombatSystem.js scripts/check-game.mjs
git commit -m "Tax and reward across the wuxing cycle, and book what every skill dealt"
```

---

### Task 8: RunManager 接线（潮汐构成/精英排程/金雨/弹道）+ HUD 潮汐行

**Files:**
- Modify: `src/run/RunManager.js`
- Modify: `src/run/RunHud.js`（潮汐字段）
- Modify: `src/core/App.js`（projectiles/tides 装配 + sync）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Consumes: `TideSchedule / EnemyProjectiles / PickupSystem.rainAt / enemies.onFire`
- Produces:

```
// RunManager 构造入参增 tides, projectiles；新公开：
tide(): { index, element, progress, timeLeft, nextElement }   // 转发 tides.tideAt(elapsed)
onShardHand: (element) => void    // 残章拾取 → App 开定向手（Task 10 消费）
// tick 内新职责（顺序固定）：
//   1) 刷怪构成：element = tides.rollElement(rng, elapsed)；behavior 按 mix 表
//      （minute≥rangedFrom 后 rangedShare 概率吐息者，tankFrom 后 tankShare 磐兽）
//   2) 精英排程：每潮 progress 越过 settings.tides.eliteAt[k] 时刷 1 只精英
//      （独立 telegraph，落点半径同普通；跨潮重置标记）
//   3) 潮末：进入新潮的那个 tick 在玩家旁 rainAt + toast 钩子 onTideTurn(element)
//   4) projectiles.tick 伤害并入 player.takeDamage；start()/clear 贯通
// 精英死亡（onDeath elite=1）→ dropAt(kind 1) + dropShard(该敌 element)
```

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- run cadence: composition, elites, rain, shard hand ---- */
{
  const rng = createRng(31);
  const enemies = new EnemySystem(rng);
  const pickups = new PickupSystem();
  const player = new PlayerState();
  const tides = new TideSchedule(createRng(31));
  const shots = new EnemyProjectiles();
  const run = new RunManager({
    enemies, pickups, player, rng, tides, projectiles: shots,
    combat: { tick: () => {}, release: () => -1, resetStats: () => {} },
    targets: { register: () => {} },
    abilities: { active: [], onRetire: null }
  });
  run.start();

  // Two simulated minutes: spawns lean the tide's colour ~bias share.
  for (let t = 0; t < 60 * 120; t++) run.tick(1 / 60, { x: 0, z: 0 });
  let tideColoured = 0;
  for (let i = 0; i < enemies.count; i++) {
    if (enemies.element[i] === tides.order[0]) tideColoured++;
  }
  // Minute 2 is inside tide one; mixed colours exist but the tide dominates.
  assert.ok(
    tideColoured > enemies.count * 0.5,
    `cadence: tide colour dominates (${tideColoured}/${enemies.count})`
  );

  // Elites: tide one is past both eliteAt marks by 120s → two elites seen.
  let elites = 0;
  for (let i = 0; i < enemies.count; i++) if (enemies.elite[i]) elites++;
  assert.equal(elites + run.kills === 0 ? 0 : elites, elites); // elites may have died; assert via scheduler flag instead:
  assert.equal(run._elitesSpawned, 2, 'cadence: both mid-tide elites scheduled');

  // Tide turn: crossing 180s rains gold near the player.
  const gemsBefore = pickups.count;
  run.elapsed = settings.tides.length - 0.01;
  run.tick(1 / 60, { x: 0, z: 0 });
  assert.ok(pickups.count >= gemsBefore + settings.tides.goldRain.count - 1, 'cadence: the turn rains gold');

  // An elite corpse drops blue + shard.
  enemies.clear();
  const boss = enemies.spawnAt(1, 0, 5, 3, 0, 1);
  const before = pickups.count;
  enemies.damage({ x: 1, z: 0 }, 1, 1e9);
  assert.equal(pickups.count, before + 2, 'cadence: elite drops blue gem and shard');
  console.log('ok  run cadence');
}
```

- [ ] **Step 2: 确认失败** — RunManager 不识 tides/projectiles

- [ ] **Step 3: 实现**

RunManager 变更（完整并入，关键段）：

```js
  // constructor 追加状态：
  this._elitesSpawned = 0;
  this._lastTideIndex = 0;
  this.onTideTurn = null;
  this.onShardHand = null;

  this.s.enemies.onDeath = (x, z, element, elite) => {
    this.kills++;
    if (elite) {
      this.s.pickups.dropAt(x, z, this.elapsed / 60, 1);
      this.s.pickups.dropShard(x, z, element);
    } else {
      this.s.pickups.dropAt(x, z, this.elapsed / 60);
    }
  };
  this.s.enemies.onFire = (x, z, dx, dz) => this.s.projectiles.spawn(x, z, dx, dz);
  this.s.pickups.onShard = (element) => this.onShardHand?.(element);

  tide() {
    return this.s.tides.tideAt(this.elapsed);
  }
```

`start()` 追加 `this._elitesSpawned = 0; this._lastTideIndex = 0; this.s.projectiles.clear(); this.s.combat.resetStats?.();`。

`tick` 内（spawn debt 循环后）：

```js
    const tide = this.s.tides.tideAt(this.elapsed);

    // Mid-tide elites: one per eliteAt mark, reset when the tide turns.
    if (tide.index !== this._lastTideIndex) {
      this._lastTideIndex = tide.index;
      this._elitesSpawned = 0;
      this.s.pickups.rainAt(playerPos.x, playerPos.z, this.s.rng);
      this.onTideTurn?.(tide.element);
    }
    const marks = settings.tides.eliteAt;
    const dueElites = (tide.index + 1) * 0; // per-tide counter below uses local marks
    if (this._elitesSpawned < marks.length && tide.progress >= marks[this._elitesSpawned]) {
      this._elitesSpawned++;
      this._queueSpawn(playerPos, tide.element, 0, 1);
    }

    // Projectiles bite through the same mercy window as contact.
    const shot = this.s.projectiles.tick(step, playerPos);
    if (shot > 0) this.s.player.takeDamage(shot);
```

（`dueElites` 行删除——上面伪注意力残留，落笔时不要写。`_elitesSpawned` 语义改为"本潮已出"，断言同步理解为潮一 120s 时=2。）

`_queueSpawn(playerPos, element = null, behavior = null, elite = 0)`：telegraph 对象扩为 `{x, z, t, element, behavior, elite}`；element 缺省时 `this.s.tides.rollElement(this.s.rng, this.elapsed)`；behavior 缺省按 mix 表：

```js
    const minute = this.elapsed / 60;
    const mix = settings.enemies.mix;
    let kind = 0;
    const roll = this.s.rng();
    if (minute >= mix.tankFrom && roll < mix.tankShare) kind = 2;
    else if (minute >= mix.rangedFrom && roll < mix.tankShare + mix.rangedShare) kind = 1;
```

telegraph 到期落敌：`spawnAt(tg.x, tg.z, minute, tg.element, tg.behavior, tg.elite)`。

RunHud：模板加 `<span data-k="tide"></span>`，`update` 增末参 `tideInfo`：

```js
    this._set('tide', `${WUXING_LABEL[tideInfo.element]}潮 ${Math.ceil(tideInfo.timeLeft)}s · 下潮 ${WUXING_LABEL[tideInfo.nextElement]}`);
```

（`WUXING_LABEL` import 自 TideSchedule。）

App：装配 `this.tideSchedule = new TideSchedule(rng); this.enemyProjectiles = new EnemyProjectiles(); this.scene.add(this.enemyProjectiles.points);`，RunManager 入参补 `tides/projectiles`；帧循环 `this.pickups.sync()` 旁加 `this.enemyProjectiles.sync();`，HUD update 传 `this.run.tide()`；`onTideTurn = (el) => this.hud.showToast(`${WUXING_LABEL[el]}潮来临`)`。重开路径（Enter）已走 `run.start()` ✓。

- [ ] **Step 4: 双脚本全绿 + 浏览器抽查**

`#run` 下观察：怪群主色随 3 分钟换潮、潮末金珠雨、精英大个发亮、吐息者远处站定吐红弹、HUD 顶部潮汐倒计时。

- [ ] **Step 5: Commit**

```bash
git add src/run/RunManager.js src/run/RunHud.js src/core/App.js scripts/check-game.mjs
git commit -m "Turn the tides: coloured waves, mid-tide elites, gold at the turn, bolts in the air"
```

---

### Task 9: 自动施法

**Files:**
- Modify: `src/core/App.js`（autocast 状态 + tick 钩 + Shift 徽章交互）
- Modify: `src/input/InputManager.js`（Shift+数字 emit `autocast`）
- Modify: `src/run/CombatSystem.js`（`ability.autocast` 折减）
- Modify: `src/abilities/FireballAbility.js`（自结算处同折减）
- Modify: `src/config/settings.js`（`run.autocastDamage: 0.85`）
- Modify: `src/ui/styles.css`（徽章蓝点样式）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Produces: `App._autocast = new Set()`（seat 下标集，run state，重开清空）；InputManager 在 `event.shiftKey && Digit1..6` 时 emit `('autocast', slot)` 且**不再**发 ability；CombatSystem `_amp` 变为 `_amp(ability)`：`mods.damageMult(ability.element) * (ability.autocast ? settings.run.autocastDamage : 1)`；`AbilityManager.cast` 返回的实例由 App 打标 `ability.autocast = true/false`（每次 cast 都写，避免池化残留——M1 勘误同型教训）。

守卫（账本要求）：seat 空（`elementAt(seat) === null`）跳过；`upgradeUi.isOpen`、verdict 非 playing、冷却未好、`enemies.nearestTo` 返回 -1 时均不发。

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- autocast: the toggle costs 15% and never fires blind ---- */
{
  // CombatSystem's amp folds the autocast tax per cast.
  const seen = [];
  const combat = new CombatSystem(
    { damage: () => 0, damageOnce: (id, p, r, amt) => (seen.push(amt), 0), slow: () => {} },
    null
  );
  const cast = {
    element: 'ice', phase: 'travel', u: 0.5, autocast: true,
    position: { x: 1, z: 0 }, origin: { x: 0, z: 0 },
    direction: { x: 1, z: 0 }, length: 4
  };
  combat.tick(1 / 60, [cast]);
  assert.ok(
    Math.abs(seen[0] - settings.combat.ice.damage * settings.run.autocastDamage) < 1e-9,
    'autocast: damage pays the 0.85 tax'
  );
  seen.length = 0;
  cast.autocast = false;
  combat.release(cast);
  combat.tick(1 / 60, [cast]);
  assert.ok(Math.abs(seen[0] - settings.combat.ice.damage) < 1e-9, 'autocast: manual stays full price');
  console.log('ok  autocast tax');
}
```

（发射守卫链在 App 内、依赖 DOM/loadout 装配，headless 不折腾——浏览器清单验证；`has(null)` 守卫以代码评审 + 浏览器空槽开自动无异常为准。）

- [ ] **Step 2: 确认失败** — `_amp` 不识 autocast

- [ ] **Step 3: 实现**

InputManager `_onKeyDown` 的 Digit 分支改造（六个 case 顶部统一拦截）：

```js
      case 'Digit1': case 'Digit2': case 'Digit3':
      case 'Digit4': case 'Digit5': case 'Digit6': {
        const slot = Number(event.code.slice(-1)) - 1;
        this.emit('action', event.shiftKey ? 'autocast' : 'ability', slot);
        break;
      }
```

（字母键 case 保持原样、不并入——沙盒键位不变，run 的 loadout 座位映射在 App。对照真实 switch 结构落笔。）

App：

```js
      case 'autocast': {
        if (!this.runMode) break;
        // slot here is the loadout seat directly (digits map 1:1 to seats).
        if (this.loadout.elementAt(slot) === null) break;
        if (this._autocast.has(slot)) this._autocast.delete(slot);
        else this._autocast.add(slot);
        this._syncBadges();
        break;
      }
```

帧循环内（run tick 之后、冻结与 verdict 守卫内）：

```js
    if (this.runMode && !this.upgradeUi.isOpen && this._verdict.value === 'playing') {
      for (const seat of this._autocast) {
        const element = this.loadout.elementAt(seat);
        if (!element || (this.cooldowns.get(element) ?? 0) > 0) continue;
        const i = this.enemySystem.nearestTo(this.character.position.x, this.character.position.z);
        if (i === -1) continue;
        this._quickCastToward(element, this.enemySystem.x[i], this.enemySystem.z[i]);
      }
    }
```

`_quickCastToward(element, tx, tz)`：复用 `_quickCast` 的选中/冷却/echo 流程，但方向与距离手工解析（对照 `_quickCast` 真实实现改写，方向 = 指向目标、线技能满射程、圈技能距离 = min(目标距离, range)），cast 返回的 ability 实例统一 `ability.autocast = auto;`（`_quickCast` 手动路径写 `false`）。徽章蓝点：`_syncBadges` 里对 `this._autocast.has(seat)` 的徽章加 class `run-tab--auto`，CSS：

```css
.run-tab--auto::after {
  content: '';
  position: absolute;
  top: 3px;
  right: 3px;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: #58aaff;
}
```

徽章 Shift+点击：现有徽章 click 处理器加 `if (event.shiftKey) { …toggle same as action… return; }`。重开（`run.start()` 路径）清 `this._autocast.clear()`。FireballAbility 自结算 damage 行乘 `(this.autocast ? settings.run.autocastDamage : 1)`。CombatSystem `_amp(ability)` 签名改造，四处调用点同步。

- [ ] **Step 4: 双脚本全绿 + 浏览器验证**

清单：Shift+1 徽章亮蓝点 → 站着不动技能自动朝最近敌人施放、飘字 ×0.85；Shift+1 再按取消；空槽 Shift+3 无反应；选卡冻结期间不自动发；死亡后不发；重开后开关清零。

- [ ] **Step 5: Commit**

```bash
git add src/core/App.js src/input/InputManager.js src/run/CombatSystem.js src/abilities/FireballAbility.js src/config/settings.js src/ui/styles.css scripts/check-game.mjs
git commit -m "Let a slot fight on its own for 15% of the pay"
```

---

### Task 10: 结算屏 + 残章定向手

**Files:**
- Create: `src/run/VerdictPanel.js`
- Modify: `src/run/PlayerState.js`（`lastHitBy` 记录）
- Modify: `src/run/RunManager.js`（受击来源贯通）
- Modify: `src/core/App.js`（verdict 走面板；残章手接线）
- Modify: `src/run/UpgradePool.js`（`draw` 支持 element 过滤）
- Modify: `src/ui/styles.css`（面板样式）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Produces:

```
// PlayerState 追加：
lastHitBy: { element: number, behavior: number } | null   // takeDamage(amount, source?) 记录
// RunManager：contact/projectile 调 takeDamage 时带 {element, behavior}
//   （contact 来源 = enemies.tick 新增输出 contactSource：{element, behavior}，
//    实现为 EnemySystem.tick 在记录最强接触时同步记下该敌两字节，暴露
//    this.lastContact = { element, behavior }；projectile 来源 behavior=1、
//    element 用 -1（弹道不带行——结算文案退化为“吐息者的弹幕”）
// UpgradePool.draw(level, sinceLevel = level, onlyWuxing = null)
//   onlyWuxing !== null 时候选仅保留 wuxingOf[element] === onlyWuxing 的
//   upgrade/new 卡（被动剔除，spec 残章语义）；空候选返回 []
// VerdictPanel：
class VerdictPanel {
  constructor(parent = document.body)
  show({ won, elapsed, kills, level, buildLines, topSkills, deathLine }): void
  hide(): void
  isOpen: boolean
}
// App：verdict !== 'playing' 时组装数据开面板（Enter 重开路径不变、面板隐藏）；
// run.onShardHand = (element) => 冻结开定向手（复用 upgradeUi.open；
//   空手兜底 = skipHeal + toast '残章逸散'）
```

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- verdict data & shard hands ---- */
{
  // The player books who hit them last.
  const player = new PlayerState();
  player.takeDamage(10, { element: 2, behavior: 0 });
  assert.deepEqual(player.lastHitBy, { element: 2, behavior: 0 }, 'verdict: last hit is booked');
  player.reset();
  assert.equal(player.lastHitBy, null, 'verdict: reset forgets the killer');

  // A shard hand only deals its own wuxing.
  const loadout = new Loadout();
  settings.run.draftLoadout = true;
  loadout.reset(); // seat 0 = ice (water, wuxing 2)
  const pool = new UpgradePool(createRng(14), loadout, new Modifiers());
  const hand = pool.draw(3, 3, 2); // water shard
  assert.ok(hand.length > 0, 'shard: water offers exist (ice upgrade / glacier new)');
  for (const card of hand) {
    assert.ok(card.kind !== 'passive', 'shard: passives sit out directed hands');
    assert.equal(settings.combat.wuxingOf[card.element], 2, 'shard: every card is water');
  }
  const none = pool.draw(3, 3, 4); // earth: no earth abilities exist yet
  assert.equal(none.length, 0, 'shard: an empty wuxing returns an empty hand');
  console.log('ok  verdict & shards');
}
```

- [ ] **Step 2: 确认失败** — `takeDamage` 单参、`draw` 无第三参

- [ ] **Step 3: 实现**

PlayerState：`this.lastHitBy = null;` 进 `reset()`；`takeDamage(amount, source = null)` 命中生效时 `this.lastHitBy = source;`（守卫路径不记）。EnemySystem `tick`：记录最强接触时同步 `this.lastContact.element = this.element[i]; this.lastContact.behavior = this.behavior[i];`（构造器初始化 `this.lastContact = { element: 0, behavior: 0 };`，零分配复用）。RunManager：`contact > 0` 时 `takeDamage(contact, this.s.enemies.lastContact)`；弹道 `takeDamage(shot, PROJECTILE_SOURCE)`（模块常量 `{ element: -1, behavior: 1 }`）。

UpgradePool.draw 第三参过滤（候选生成后、抽取前）：

```js
    if (onlyWuxing !== null) {
      for (let i = candidates.length - 1; i >= 0; i--) {
        const card = candidates[i].card;
        const keep =
          card.kind !== 'passive' &&
          settings.combat.wuxingOf[card.element] === onlyWuxing;
        if (!keep) candidates.splice(i, 1);
      }
    }
```

VerdictPanel（DOM，样式沿 run-hud 灰盒风）：

```js
// src/run/VerdictPanel.js
/**
 * The end card (spec §7 结算): stats, the build, the top three skills and one
 * line about what killed you — plus the advice the wuxing table implies. Grey
 * DOM for now; M5 dresses it.
 */
export class VerdictPanel {
  constructor(parent = document.body) {
    this.root = document.createElement('div');
    this.root.className = 'verdict';
    this.root.style.display = 'none';
    parent.appendChild(this.root);
    this.isOpen = false;
  }

  show({ won, elapsed, kills, level, buildLines, topSkills, deathLine }) {
    const s = Math.floor(elapsed);
    const time = `${String((s / 60) | 0).padStart(2, '0')}:${String(s % 60).padStart(2, '0')}`;
    this.root.innerHTML =
      `<h2>${won ? '生存达成' : '倒下了'}</h2>` +
      `<p>存活 ${time} · 击杀 ${kills} · Lv ${level}</p>` +
      `<p class="verdict__build">${buildLines.join('<br>')}</p>` +
      (topSkills.length
        ? `<p>输出前三：${topSkills.map(([name, amount]) => `${name} ${Math.round(amount)}`).join(' / ')}</p>`
        : '') +
      (deathLine ? `<p class="verdict__death">${deathLine}</p>` : '') +
      `<p class="verdict__hint">回车重开</p>`;
    this.root.style.display = '';
    this.isOpen = true;
  }

  hide() {
    this.root.style.display = 'none';
    this.isOpen = false;
  }

  dispose() {
    this.root.remove();
  }
}
```

CSS（styles.css 追加）：

```css
.verdict {
  position: fixed;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  background: rgba(4, 7, 12, 0.82);
  color: #e8eef6;
  font: 500 15px/1.6 system-ui, sans-serif;
  text-align: center;
  z-index: 40;
}
.verdict h2 { font-size: 30px; letter-spacing: 0.2em; }
.verdict__death { color: #ff8a76; }
.verdict__hint { opacity: 0.6; }
```

App 组装（verdict 分支替换现 `showVerdict` 文本路径；对照真实代码锚点）：

```js
        const t = this.run.tide();
        const killer = this.playerState.lastHitBy;
        let deathLine = null;
        if (!won && killer) {
          if (killer.element < 0) deathLine = '死于：吐息者的弹幕——它们怕近身';
          else {
            const beats = WUXING_LABEL[BEATS.indexOf(killer.element)];
            deathLine =
              `死于：${WUXING_LABEL[killer.element]}系${['涌兽', '吐息者', '磐兽'][killer.behavior]}` +
              `——${beats}系对${WUXING_LABEL[killer.element]}系有 1.25× 克制`;
          }
        }
        const topSkills = Object.entries(this.combat.damageDealt)
          .sort((a, b) => b[1] - a[1])
          .slice(0, 3)
          .map(([el, amt]) => [ELEMENT_META[el]?.label ?? el, amt]);
        this.verdictPanel.show({
          won,
          elapsed: this.run.elapsed,
          kills: this.run.kills,
          level: this.pickups.level,
          buildLines: this._buildSummaryLines(), // _buildSummary 改产数组并换 ELEMENT_META label（M2 minor 就地清偿）
          topSkills,
          deathLine
        });
```

Enter 重开路径补 `this.verdictPanel.hide();`。残章手：`this.run.onShardHand = (element) => { const hand = this.upgradePool.draw(this.pickups.level, this.pickups.level, element); if (!hand.length) { /* skipHeal + toast 残章逸散 */ } else { /* upgradeUi.open(hand, { rerolls: 0, summary: … })，choice 回 _onUpgradeChoice 同路 */ } };`（冻结语义与升级手完全一致；对照 `_onUpgradeChoice`/pendingLevels 消费实现落笔，残章手不消耗 pendingLevels）。

- [ ] **Step 4: 双脚本全绿 + 浏览器验证**

清单：故意送死 → 结算屏含存活/击杀/等级/build/输出前三/死因（带克制建议）→ 回车重开；打满 15 分钟（`timeScale` 拉高）→ 胜利版结算；杀精英捡白色残章 → 冻结 → 只出该系卡；土系残章（现无土技能）→ 回血兜底 + toast。

- [ ] **Step 5: Commit**

```bash
git add src/run/VerdictPanel.js src/run/PlayerState.js src/run/RunManager.js src/run/UpgradePool.js src/core/App.js src/ui/styles.css scripts/check-game.mjs
git commit -m "End the run with a verdict worth reading, and let shards deal a directed hand"
```

---

### Task 11: 收尾（混编压测 / README / 勘误位）

**Files:**
- Modify: `scripts/check-game.mjs`（压测改混编）
- Modify: `README.md`（Run mode 段落 M3 内容）
- Modify: `docs/superpowers/plans/2026-08-15-m3-cadence.md`（文末勘误节，执行会话填写）

- [ ] **Step 1: 压测断言升级为三行为混编**

现有 stress 块的 300 涌兽改为 210 涌兽 + 60 吐息者 + 30 磐兽（`spawnAt` 带 behavior），并在循环里同步 `projectiles.tick`（吐息者会真开火）；预算维持 `< 2ms`，输出行加注行为构成。

- [ ] **Step 2: 双脚本全绿**

Run: `npm run check:game && npm run check && npm run sim`
Expected: 全绿；sim 难度带不受本里程碑影响（模型未变），照旧 新手0% / 基线~59% / 熟练100%。

- [ ] **Step 3: README run 段补 M3**：潮汐/精英/残章/自动施法（Shift+数字）/结算屏，一段话 + 键位行。

- [ ] **Step 4: 浏览器全流程验收清单（执行会话逐项打钩）**

1. 开局涌兽 → 1.5 分钟后出现吐息者远程站桩吐红弹，空格可穿弹 ✓
2. 3 分钟磐兽入场：慢、硬、推不动 ✓
3. 潮汐换色：主色随潮变、HUD 潮行倒计时、潮末金珠雨 + toast ✓
4. 精英每潮两只：大个发亮、蓝宝石全场飞来、白残章掉落 ✓
5. 残章定向手只出该系；无卡兜底回血 ✓
6. 克制手感：对被克色打出更大飘字（×1.25），反向变小 ✓
7. Shift+数字挂机位工作、×0.85、选卡/死亡不发 ✓
8. 死亡/胜利结算屏数据齐全、死因带克制建议、回车重开一切归零 ✓
9. 去掉 `#run`：沙盒逐帧如常 ✓
10. 300 混编压测（编辑器拉 spawnBase+godMode）帧预算不破 ✓

- [ ] **Step 5: Commit**

```bash
git add scripts/check-game.mjs README.md docs/superpowers/plans/2026-08-15-m3-cadence.md
git commit -m "Stress the mixed horde and write the cadence down"
```

---

## Self-Review 结论（已执行）

- **Spec 覆盖**：§7 潮汐/精英/终潮→T2/T8；§5 行为模板/弹道红系→T3/T4；精英+残章→T5/T6/T10；§1 克制倍率→T7；§6 宝石分级/金雨→T6；自动施法→T9；结算（死因+建议+top3）→T10；账本义务全部落 T1（含冻结冷却）。印记减益/共鸣/融合按分期属 M4，不在本计划。
- **占位符扫描**：无 TBD；App 接线处按规程①标注"对照真实实现落笔"的均附带了行为定义与锚点描述；T8 Step 3 中错写的 `dueElites` 行已在文内显式标注删除。
- **类型一致性**：`spawnAt(x, z, minute, element, behavior, elite)` 在 T3/T5/T8/T11 一致；`onDeath(x, z, element, elite)` T5/T8 一致；`draw(level, sinceLevel, onlyWuxing)` T1/T10 递进兼容；`damage(point, radius, amount, wuxing)` T7 与 Targets 透传一致；`WUXING_LABEL/BEATS` 均出自 TideSchedule 单一来源。

## 执行后勘误（执行会话填写）

（留空）
