# M6 量产与发布 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1 收官：13 个首发新技能（含土系登场——解锁周天/土禁咒/双融合对/castEarth）经 6 个类（3 模板 + 3 特例）落地、战术档法力消费、护盾机制、编辑器通用折叠、资产瘦身、门面三件套、发布回归清单与 v1 标记。

**Architecture:** 新技能 = 数据行（settings 全量参数）× 少数类：`LineSweepSkill / ZoneBurstSkill / OrbitAuraSkill` 三模板吃掉 9 个，`ShieldSkill(×2) / DashStrikeSkill / ChainBoltSkill` 三特例吃掉 4 个；VFX 全部组合既有系统（粒子预设/实例晶体/SDF 贴花/BurstSphere/丝带）按五行染色。战斗侧只新增 `aura` 与 `shield` 两个语义。现有 7 个手写技能一行不动。

**Tech Stack:** 不变；资产处理只用 macOS 自带 `sips`（零新依赖）。

## Global Constraints（含 M1–M5 全部勘误规程）

- 规程①对照真实文件核对锚点（对账清单 Task 1）；规程②新公开逻辑必附会失败断言；规程③settings 与读者同任务
- 修正层铁律；固定 60Hz tick；热路径零分配；沙盒逐帧不变；`death wins the tick`
- 五字段施放不变量（cooldown/`_applySequence`/`autocast`/`fusionMult`/`quenched`）扩为**六**：法力 spend 是施放门（pre-cast gate），不是 ability 字段——门失败则整次施放不发生
- 新技能伤害必须过 **锚2 公式带**（`20 × (冷却/1.2) × 形状系数`，±40% 断言护栏——spec §8 的防手滑条款在本里程碑正式落地）
- 新 UI 文案一律 `t()`；报告中派单授权的选择必须标注 **dispatch-authorized**（M5 流程教训）
- commit 自然句式 + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

**计划内裁定**（用户可否决）：
1. **5 个融合专属 VFX → post-v1 backlog**：合成施放已满足 spec 机制与 ×1.2 预算且视觉上是两发全额技能齐放；13 技能才是本里程碑的山。
2. **首发 13 技能名单**（spec §12 v1 名单）：金=万剑诀/剑域/弑神一闪 · 木=连锁闪电/生命绽放 · 水=寒霜新星/冰晶甲 · 火=燃阵/日轮 · 土=岩刺突贯/落石/震地波/石肤。
3. **护盾机制**：`PlayerState.shield` 吸收池（先于 HP 扣），限时衰减；石肤附接触反伤。HP 瓶液面上叠白色护盾带。
4. **战术档法力**（spec 锚2.5）：弑神一闪/寒霜新星/冰晶甲/震地波/石肤 = 30 蓝；其余新技能 0 蓝。
5. **资产预算修订**：spec 首载 <10MB 在无 draco/ktx2 依赖下不可达——v1 目标改为**削去可白拿的 ~14MB**（孤儿模型/贴图转 JPEG/大纹理降采样/preload 门控），<10MB 立为 post-v1（named path: draco+ktx2）。
6. **游戏名保持占位"五行降世"**（用户命名权开放，门面用占位名）。
7. **v1 完成后打本地 tag `v1.0`**（用户负责推送）。

---

### Task 1: 对账 + M5 门面清偿

**Files:** `src/ui/HUD.js`（删 setRunKeys）/ `src/core/App.js`（角色切换 toast 走 t()）/ `src/ui/strings.js`（新键 + en 汉字间距修）/ `src/run/OrbBottles.js` + `src/effects/ScreenFlash.js` + `src/core/App.js`（reduceFlashes 0.5→0.2，spec §9.5 −80% 对齐）/ `scripts/check-game.mjs`

- 对账清单（grep 逐项）：`ABILITY_TYPES` 注册表形状 / `ELEMENTS` 七元素 / `ELEMENT_META` 字段（label/accent/key/hint/cast）/ `glyphs.js` WRAP+常量导出模式 / `CastShape.LINE|ZONE` / Ability 基类 hooks（createShaders/createParticles/onSpawn/onTravel/onImpact/onFade/onDestroy）/ `settings.combat` kinds / UpgradePool new-候选来源 / TitleScreen HOME_ELEMENT 表 / `PlayerState` 字段。
- reduceFlashes 因子统一为常量 `FLASH_DAMP = 0.2`（settings.ui 注释同步），三消费点齐改；断言钉 0.2。
- en 间距：`金Tide` → `Metal tide`（潮行/共鸣行英文用 WUXING 英文词——T1 只修间距与英文词，中文照旧字形）。
- [ ] 断言先失败 → 实现 → 双套件绿 → Commit `Pay the facade debts and damp the flashes to spec`

---

### Task 2: 十三技能的数据地基

**Files:** `src/config/settings.js` / `src/ui/glyphs.js` / `src/run/TitleScreen.js`（土卡启用）/ `scripts/check-game.mjs`

**Produces（后续任务按名引用）：**
- `ELEMENTS` 增 13 id：`swordrain, bladeorbit, dashstrike, chainbolt, lifebloom, frostnova, iceshield, firering, sunwheel, rockspikes, boulder, quake, stoneskin`
- `ELEMENT_META` 13 行（label 英文名 + accent 五行色系 + cast: LINE/ZONE/SELF——`CastShape` 增 `SELF`（自身施放不瞄准：寒霜新星/震地波/剑域/日轮/燃阵/冰晶甲/石肤）；`castShapeOf` 默认不变；AimController 对 SELF 短路（quickCast 直接以自身为原点发 cast，方向任意））
- 每技能 settings 块：VFX 参数（颜色由 wuxing 派生 + 每技能微调）+ `cooldown`（档位：速射 1.2 连锁闪电 / 标准 2–4 万剑诀·生命绽放·燃阵·日轮·岩刺·落石·剑域 / 战术 6–10 弑神一闪·寒霜新星·冰晶甲·震地波·石肤）+ `manaCost`（战术档 30 其余 0——`settings.combat.manaCosts` 或每块内 `manaCost` 字段，选每块内）
- `settings.combat` 13 行：kinds — swordrain `burst` / bladeorbit `aura` / dashstrike `self`（特例自结算）/ chainbolt `self`（特例自结算）/ lifebloom `burst`(heal 变体：`healPlayer` 字段) / frostnova `burst`(self) / iceshield `shield` / firering `aura` / sunwheel `aura` / rockspikes `sweep` / boulder `burst`(stun=大 slow) / quake `burst`(self+knockback 大) / stoneskin `shield`；`wuxingOf` 13 行（金金金木木水水火火土土土土）
- **锚2 公式带断言**：遍历全部 20 个可折算技能（7 旧 + 13 新，shield/self 特例豁免名单显式列出），`DPS 折算 ∈ 基准 ±40%`——防手滑护栏正式上岗
- glyphs：13 个内联 SVG sigils（沿 WRAP 模式，每系底纹一致 + 每技能一个辨识元素；100×100 stroke-only）
- TitleScreen：土卡启用（首技 `rockspikes`）；`HOME_ELEMENT` 表补土行
- [ ] 断言先失败（ELEMENTS 长度 20 / 公式带 / manaCost 战术档五个 30）→ 数据落位 → 双套件绿 → Commit `Table the thirteen: names, numbers, sigils, the earth card`

---

### Task 3: 法力消费接线

**Files:** `src/core/App.js` / `src/run/RunHud.js` / `src/ui/styles.css` / `scripts/check-game.mjs`

- 施放门：`_quickCast`/`_quickCastToward`/`_cast`（run 模式）在冷却检查旁增 `manaCost > 0 ? playerState.spendMana(cost) : true` 门——门败则不施放并 toast `t('run.noMana')`（限流）；demo 施放免蓝；融合取双亲 manaCost 之和？裁定：融合成本 = max(双亲)（与冷却同哲学），注释。echo 复发免蓝（echo 是被动赠礼）。autocast 蓝不足静默跳过（不 toast）。
- HUD：T6(M5) 预留的蓝点接真数据（manaCost>0 的槽显示）；蓝不足时槽位蓝色蒙层（区别于 CD 暗色扫掠——蒙层 class 由 App 每帧喂 mana 状态）。
- 断言：spend 门语义（不足不放不扣冷却——冷却写与蓝扣的顺序：先蓝后冷却，败则两者皆无）。
- [ ] 断言 → 实现 → 双套件绿 + 浏览器（战术技能蓝不足灰显不放）→ Commit `Make the heavy arts drink from the blue bottle`

---

### Task 4: 三模板类 + 九技能落地

**Files:** Create `src/abilities/templates/LineSweepSkill.js` / `ZoneBurstSkill.js` / `OrbitAuraSkill.js`；Modify `src/abilities/AbilityManager.js`（注册表 13 行）/ `src/run/CombatSystem.js`（`aura` kind + burst 的 healPlayer/stun 字段）/ `scripts/check-game.mjs`

- **LineSweepSkill**（岩刺突贯）：冰枪骨架的数据化变体——实例化尖刺沿线依次拔起（ProceduralGeometry 晶簇，土色岩理由 wuxing 派生色+粗糙参数近似）、SDF 地裂贴花、粒子尘。参数：spikeCount/width/riseTime。
- **ZoneBurstSkill**（万剑诀/生命绽放/寒霜新星/落石/震地波）：落点（或自身）圈爆发的参数化家族——preEffect(剑雨=实例化细刃下坠 0.4s / 落石=单大石下坠 / 其余无) + 爆发（BurstSphere 模式选择 + 粒子预设 + SDF 环贴花）+ combat burst 行付伤害；frostnova/quake `self: true` 以施放者为圆心。
- **OrbitAuraSkill**（剑域/燃阵/日轮）：环绕体（实例化飞剑 ×5 / 地面火环 SDF+粒子 / 公转火球 ×3 走 BurstSphere 小径）公转于玩家，**装备即常驻**（spec §4.5 光环规：无冷却无施放——实现：入席即 spawn 一个 permanent cast，退席/局末 destroy；CombatSystem `aura` kind：以玩家为圆心 radius 环带 tick 伤害 ×0.7 形状系数）。App：席位变化时同步常驻光环（`_syncAuras()`——acquire/fuse/restart 调）。光环不占施放键：徽章显示"常驻"状态（小图标替代 CD 环）。
- CombatSystem：`aura` kind（读 ability.position=玩家、环带 damage tick）；`burst` 增可选 `healPlayer`（RunManager 路由 heal——lifebloom）与 `stunTime`（大 slow 1.0）。
- 断言：aura 环带 tick 数学 / healPlayer 路由 / 模板类实例遵守池化合约（headless 只测 combat 行；模板 VFX 浏览器验收）。
- 浏览器：九技能逐个 editor 全席试放（外观辨识度 + 判定所见即所判 + 光环常驻/退席消失）。
- [ ] 断言 → 实现 → 双套件绿 + 浏览器逐技能 → Commit `Three molds, nine spells: sweep, burst and orbit`

---

### Task 5: 护盾特例（冰晶甲/石肤）

**Files:** Create `src/abilities/templates/ShieldSkill.js`；Modify `src/run/PlayerState.js` / `src/run/RunManager.js`（反伤路由）/ `src/run/OrbBottles.js`（护盾带）/ `src/run/CombatSystem.js`（shield kind：施放时 `player.addShield(amount, duration)`）/ `scripts/check-game.mjs`

- PlayerState：`shield`/`shieldT`；`addShield(amount, duration)`（覆盖式取大）；`takeDamage` 先扣盾（盾不触发无敌帧；穿盾余量进 HP 才触发）；tick 衰减到期清零。石肤：`reflectShare` 参数——接触伤害被盾吸收时按份额反伤攻击者（RunManager 在接触路径带 lastContact 索引反打——`enemies.damage` 单点小半径）。
- ShieldSkill VFX：环绕玩家的晶体环（冰=晶蓝透明 / 石=岩色粗粝），存续期随 shieldT，破盾碎裂粒子。
- OrbBottles：HP 瓶液面上白色护盾带（`uShield` ratio uniform）。
- 断言：吸收顺序/穿盾余量触发 iframes/到期清零/反伤路由/覆盖取大。
- [ ] 断言 → 实现 → 双套件绿 + 浏览器（开盾抗一波、破盾碎裂、石肤反伤飘字）→ Commit `Raise the crystal and stone shields before the skin`

---

### Task 6: 冲刺与连锁特例（弑神一闪/连锁闪电）

**Files:** Create `src/abilities/templates/DashStrikeSkill.js` / `ChainBoltSkill.js`；Modify `src/core/App.js`（dashstrike 的位移接线——复用翻滚位移通道）/ `scripts/check-game.mjs`

- **弑神一闪**：短线瞬移斩——玩家沿瞄准方向位移 dashDistance（复用 `_dodgeVec` 位移积分通道 + i 帧），路径上敌人吃暴击级伤害（damageOnce 沿线采样 ×1.5 内置），VFX：残影丝带 + 斩击闪光。combat kind `self`（类内自结算走 `ctx.targets` + `ctx.stats`——沿 fireball 模板）。
- **连锁闪电**：首目标（瞄准方向最近敌）命中后跳 4 次（每跳找 6m 内未命中过的最近敌，`damage` 单点 + 丝带段间弧光），衰减 15%/跳。类内自结算。
- 断言：链跳数学（去重/半径/衰减，用注入 fake targets headless 驱动类的结算函数——结算逻辑抽纯函数 `chainHops(enemies, from, hops, radius)`）；dash 位移量。
- [ ] 断言 → 实现 → 双套件绿 + 浏览器 → Commit `One flash steps through the line, one bolt walks the crowd`

---

### Task 7: 土系解锁全验证

**Files:** `scripts/check-game.mjs`（解锁断言）/ 小修散点（发现即修，报告列明）

- 断言/验证：`wuxingOf` 含土（4）后——`wuxingRep(4)` 反查到岩刺（top3 记账活了）/ 周天 `cycleActive()` 可达（五系各一 loadout 计算）/ 融合表 `4+0` 锋岩星阵 与 `3+4` 地心火山 eligible 路径（Loadout 五行对检测）/ 禁咒 earth 分支已有 headless pin（M5 T3）+ 本命土（标题土卡→ultimate.wuxing=4）/ castEarth 音效键触发。
- 浏览器：土本命开局全流程一遍（岩刺开局→挂土印→金技引爆淬炼→土禁咒天崩）。
- [ ] 断言 → 修散点 → 双套件绿 + 浏览器 → Commit `Earth takes the field and every dormant gate opens`

---

### Task 8: 编辑器通用技能折叠

**Files:** `src/ui/Editor.js` / `scripts/check-game.mjs`（无——纯编辑器，浏览器验收）

- 一个 `_buildGenericSkill(element)` 从 settings 块反射生成折叠（数值→滑杆、颜色→取色器、castAnim 下拉沿用），13 个新技能循环注册；现有 7 个手写折叠不动。滑杆范围推断规则注释说明（值×0.1–×3 区间）。
- [ ] 实现 → 浏览器（任一新技能拖参实时生效）→ Commit `Fold the thirteen into the editor without thirteen folders`

---

### Task 9: 资产瘦身

**Files:** `public/models/`（删孤儿）/ `src/animation/CharacterController.js`（贴图路径）/ `index.html`（preload 门控）/ `scripts/check-game.mjs`（资产存在性断言更新）

- 删 `idle2.fbx`（孤儿——CHARACTERS 已用 `Standing Idle.fbx`；grep 全仓确认零引用后删）。
- `diffuse2.png`(6M)/`diffuse.png`(1.3M) → `sips -s format jpeg -s formatOptions 80` → `.jpg`（diffuse 无 alpha；CHARACTERS.texture 路径改 .jpg；原 png 删）。cathedral `normal.jpg`(3M)/`color.jpg`(2M) → `sips -Z 1024` 降采样（Ground 观感浏览器复核）。
- preload 门控：三个 `<link rel=preload>` 改由 gate 脚本在 `__gateOk` 时注入（M5 T13 遗留）。
- 记账：build 前后 `du` 对比进报告（预期 58M → ~44M；裁定 5 的修订入勘误）。
- [ ] 实现 → `npm run check`（贴图路径）+ build + 浏览器外观复核 → Commit `Shed fourteen megabytes nobody would have seen`

---

### Task 10: 门面三件套

**Files:** `index.html` / `public/og.png`（新）/ `README.md` 头部徽章行（可选）

- favicon：内联 SVG data-URI（五行五色圆环纹——手写 20 行 SVG）；`<title>` 五行降世 · Wuxing Descends。
- og:image：dev 服务器开局打一帧华丽画面截图存 `public/og.png`（≤300KB，1200×630 裁剪）+ `og:title/og:description/twitter:card` meta。
- [ ] 实现 → 浏览器（tab 图标/查 meta）→ Commit `Give the door a face: favicon, banner, name`

---

### Task 11: 发布回归 + v1 收官

**Files:** `README.md` / `docs/superpowers/plans/2026-08-16-m6-arsenal.md`（勘误）/ `scripts/check-game.mjs`（整备）

- 三套件 + **spec §9.5 十项回归清单逐项**（新档开局/能死/能赢/能升级/能融合/双角色/双语/减闪/性能模式/刷新恢复）+ M6 增补项（13 技能各放一次/护盾/法力门/土全链/光环常驻/资产加载无 404）。
- sim 复核：13 技能改变实际 build 空间但 sim 模型抽象不变——带内即过，越带停下报数。
- README：M6 段 + 首发 20 技能表。
- 勘误节 + `git tag v1.0`（本地，用户推送）。
- [ ] 全项 → Commit `Twenty spells, one seal: v1 stands` → tag

---

### Task 12: Lv3/Lv5 质变节点（补排——spec §4.5 欠账，用户 2026-08-16 确认技能等级质变模型；执行顺序在 T7 之后、T8 之前）

**Files:** `src/config/settings.js`（每技能 `breakpoints` 块）/ Create `src/run/breakpoints.js`（解析器）/ `src/run/CombatSystem.js` + 模板类（消费）/ `src/core/App.js`（castTwice 钩 + 卡面文案）/ `src/run/UpgradePool.js`（升级卡 body 显示下一质变）/ `src/ui/strings.js` / `scripts/check-game.mjs`

**机制契约：**
- 每技能 settings 块 `breakpoints: { lv3: {…}, lv5: {…} }`——值为**乘子/增量映射**，键对应该技能 combat 行或 VFX 参数（`width: 1.5` = ×1.5；`hops: +2` = 加法，加法键以 `+` 语义在解析器中列名单）。
- `src/run/breakpoints.js`：纯函数 `bpScale(element, param, level)` / `bpAdd(element, param, level)`——按 level≥3/≥5 累乘/累加；无表项恒等。CombatSystem 伤害/宽度/半径读数处过一遍；模板类 spawn 时读形状参数同理。loadout 等级经注入的 `levelOf` 回调（沙盒无 loadout → 恒 Lv1 恒等，沙盒零变化）。
- 仅两个代码钩（数据表达不了的 Lv5 机制）：`castTwice`（冰枪 L5 双联——cast 路径读 flag 补发第二发小夹角）与 `extraWave`（陨石 L5 二次落石——burst kind 读 flag 复爆一次）；其余全数据。
- 升级卡 body：`升级 · Lv3` 时追加该级质变一句话（strings 键 `bp.<element>.lv3/lv5`，20 技能 ×2 短句 zh/en）。

**首发 20 质变表**（lv3 → lv5，一行一技能；数值为乘子/增量）：
- 冰枪 ice：宽 ×1.5 → `castTwice`（双联 ±8°）
- 雷枪 thunder：宽 ×1.4 → 伤害 ×1.3 且减速挂载（slowFactor 0.3/1s 新增）
- 陨石 meteor：半径 ×1.3 → `extraWave`（0.5s 后二次落石 ×0.6）
- 光束 beam：宽 ×1.5 → dps ×1.35
- 电缚 snare：半径 ×1.3 → slowFactor 0.45→0.65
- 冰川冠 glacier：半径 ×1.3 → slowTime ×1.6
- 火弹 fireball：爆炸半径 ×1.35 → damage ×1.3（分裂弹属 post-v1）
- 万剑诀 swordrain：剑数 +4 → 半径 ×1.4
- 剑域 bladeorbit：刃数 +2 → 环带半径 ×1.3
- 弑神一闪 dashstrike：冲程 ×1.3 → 伤害 ×1.4（处决线并入 post-v1 被动）
- 连锁闪电 chainbolt：跳数 +2 → 衰减 15%→8%
- 生命绽放 lifebloom：治疗 ×1.5 → 半径 ×1.4
- 寒霜新星 frostnova：半径 ×1.35 → slowTime ×1.5
- 冰晶甲 iceshield：盾量 ×1.4 → 时长 ×1.5
- 燃阵 firering：环带宽 ×1.35 → dps ×1.35
- 日轮 sunwheel：球数 +1 → 公转速 ×1.3
- 岩刺突贯 rockspikes：宽 ×1.5 → 伤害 ×1.35
- 落石 boulder：半径 ×1.3 → stunTime ×1.6
- 震地波 quake：半径 ×1.35 → 击退 ×1.5
- 石肤 stoneskin：盾量 ×1.4 → 反伤份额 ×1.6

**断言**：bpScale/bpAdd 恒等（Lv1/2）、Lv3 生效、Lv5 累计、无表项恒等、加法键名单；冰枪 castTwice 与陨石 extraWave 的 combat 侧行为各一条。锚2 公式带断言以 **Lv1 基值**为准（质变是投资回报，不进带）。

**浏览器**：冰枪升到 5 亲眼双联；万剑诀 Lv3 剑变多；升级卡 Lv3/Lv5 显示质变文案；沙盒零变化。

- [ ] 断言 → 实现 → 双套件绿 + 浏览器 → Commit `Let every spell break its shape at three and its rules at five`

---

## Self-Review 结论（已执行）

- **Spec 覆盖**：§12 v1 主动 20（7+13 名单逐一入 T2/T4/T5/T6）｜实现分层 6 类 ✓｜锚2.5 CD 档位与战术档法力 → T2/T3｜锚2 ±40% 护栏 → T2｜光环常驻规 → T4｜周天/土禁咒/双融合对/castEarth → T7｜§9.5 回归清单 + reduceFlashes −80% → T11/T1｜门面 → T10｜资产（预算修订裁定 5）→ T9。5 融合专属 VFX post-v1（裁定 1）。
- **占位符扫描**：无 TBD；模板类合约以行为定义 + 浏览器清单为体（M5 模式）。
- **类型一致性**：13 id 在 T2 定义全计划引用；`CastShape.SELF`/`aura`/`shield` kinds T2 定 T4/T5 消费；`addShield/spendMana` 签名 T3/T5 一致。

## 执行后勘误（执行会话填写）

**计划内裁定 1–7 落地实况**：
- 裁定1（5 个融合专属 VFX → post-v1）：如期搁置，本里程碑全程复合施放走"双亲全额齐放"路径，T6 验收两次真实融合（霜刃洪流/锋岩星阵）均确认。
- 裁定2（首发 13 技能名单）：13 id 逐一入 T2/T4/T5/T6，T11 验收 20/20 全数过手（含护盾/光环/自结算特例），无缺项。
- 裁定3（护盾机制）：如期落地，T5 一次落地 + 一次修复轮（29087a3）；T11 验收吸收顺序/穿盾余量/覆盖取大三项复核，数字精确（40 吸 25 剩 15，穿盾 10 精确进 HP）。
- 裁定4（战术档法力 5×30 蓝）：如期落地，T3 一次通过；T11 验收蓝量门（10<30）拒放三信号齐全（不扣蓝/不进冷却/不生成实例）+ 真实 toast "蓝量不足"。
- 裁定5（资产预算修订）：**估算与实际不符，按实际记账**——计划文本预期"削去可白拿的 ~14MB"，T9 实测 `public/` 44M→34M（−10M）、生产构建 58M→47M（−11M），cathedral 降采样是字节级空操作（本来就是 1024）；真实节省全部来自孤儿 idle2.fbx 删除 + diffuse/diffuse2 PNG→JPEG。裁定文本的"14MB"是估算上限，非实测值，T11 补记实际数字。
- 裁定6（游戏名占位）：`五行降世 · Wuxing Descends` 沿用至 v1（T10 门面落地时定稿字面，未再改名）。
- 裁定7（v1 完成后打本地 tag `v1.0`）：**本任务未执行**——controller 在 T11 派发文本中显式覆盖计划文本："do NOT run `git tag v1.0` — the controller holds the tag until after the milestone-final whole-branch review"。T11 只完成回归 + README + 勘误 + 一次提交；tag 留给里程碑终审后由 controller 执行。计划文本本身未错，只是执行顺序被后置。

**修复轮次**（评审中定位并现场修复的缺陷）：
- D-M6-1（T2，BLOCKED→裁定，f56eb90 前）：锚点重校准到"冰枪实际手感"而非 spec §8 纸面值——live `BASE_DPS=50`，旧 7 技能的冷却是按纸面值下调过约 3× 的手感调参结果；本轮只挪冷却轴，伤害数字不动。13 个新技能按同一活锚 ×3 重算，六个战术档技能带内乘积精确落在 1.00（评审手算复核）；旧 7 技能豁免并加注释存档。
- T2 fix round（f56eb90）：`ELEMENTS` 的第 4 个消费点（编辑器 loadout 下拉）当时漏挡，补 `ABILITY_TYPES` 过滤；陈旧的"土系暂缺"注释一并清理。
- D-M6-2（T5，29087a3，机制级裁定，FLAG FOR USER）：护盾吸收原本会绕过 `EnemySystem.tick` 自己"每 ~0.5s 一口"的接触伤害设计锚点（吸收不触发无敌帧，导致护盾在 60Hz 下被连续咬穿——冰晶甲 40 点在约 83ms 内熔穿的实测）。裁定新增 `contactMercyT`（独立于 iframes 的接触专属宽限窗，时长复用 `settings.run.iframes`，不新增旋钮）：任意落地接触（护盾/HP/穿盾）armed 该窗，窗内再接触整口吃掉；弹道类攻击不检查也不 arm 它。"盾不触发无敌帧"的原设计保留。
- T5 band 修复（同 29087a3）：护盾带渲染原先会在残血时露出裸瓶颈（`shieldTop` 只读满液位），改为 `min(fillTop, liquidTopBase+bandH)` 锚定到*实时*液面；晃动位移在锚定之后叠加。
- T6 fusion-live discovery（21c4763）：实现者原报告"弑神一闪无合法相生搭档"是方向判断错误——T2 数据其实已经上了两条含 dashstrike 的融合对（霜刃洪流 {dashstrike,iceshield}、锋岩星阵 {rockspikes,dashstrike}）。评审阶段补 headless pin + 浏览器复核两条融合真实成键、真实齐放；T11 用第三条融合对（霜刃洪流 beam+ice）再次复核，`_quickCastToward` 一次施放双亲 damageDealt 同时非零。
- T6 resolveTarget 修复（同 21c4763）：连锁闪电原实现在委中即锁定目标数组下标，目标死亡/位移后下标可能指向别的敌人（WYSIWYG 破绽）。改为 commit-once-validate-at-impact：只在目标死亡/超距时才重新搜索，随 id 追踪，4 条 headless 场景钉死。
- T6 dash/dodge 互斥（同 21c4763）：`_dashing` 标志位补上双向互斥——冲刺途中翻滚、翻滚途中冲刺均在法力门之前拒绝，不扣任何资源；融合复合施放的两种触发顺序都验证过。
- T4 quake 击退死代码（评审遗留一条，controller 现场收口 bd7aa5a，非 implementer 修复）：`combat.quake.knockback:9` 自 T2 数据落地起就没有消费端。controller 直接补 `EnemySystem.knockback` 扫描（镜像既有 `slow()` 写法），burst 分支在 stunTime 之后复合调用，断言钉死"额外冲量 = 基线 × 9/4"。T11 验收复核：quake 单发 kbX 从 0 变为非零，knockback 数值行为正常。
- T12 fix round（a096be2）：弑神一闪 Lv3+ 冲程缩放原本有两处独立公式（施法距离一处、`App._dashDisplace` 位移一处），Lv3 起两者会读到不同的数（WYSIWYG 破绽）。改出单一导出函数 `scaledDashRange(level)`，施法/位移/残影带/斩击闪光四个消费点全部读同一个值。T11 验收：Lv1 位移精确 8m（`scaledDashRange(1)=8`），伤害 280 精确匹配 `settings.dashstrike.damage`。
- T12 shield-gate watch item（T5 遗留，T12 验收时 CLOSED）：冰晶甲 Lv3（40×1.4=56）短暂超过石肤基值（55）会让石肤刷新在 take-max 规则下变成"丢反伤份额的空操作"——裁定 ACCEPT（该窗口在几秒内随衰减自然消失，两边数值都已入档）。T11 验收复核：iceshield/stoneskin 各自独立施放，覆盖取大机制未见异常。

**计划文本缺陷 / 实现者判断记录**：
- T1：`FLASH_DAMP=0.2` 的第三个消费点实际落在 `CharacterController`（不在 brief 原始文件清单里）——dispatch-authorized 扩列，三处消费点断言全部钉 0.2。T11 用真实 `ScreenFlash.trigger()` 复核：`reduceFlashes` 开启时强度精确读 0.2（不是 M5 时代的 0.5），关闭时精确读 1——spec §9.5 的 −80% 这次是字面达成，不再是"仓库惯例覆盖 spec"。
- T3：报告里"蓝点与低蓝蒙层撞色（同一 RGB 家族）"记在案但未处理——纯视觉冗余，非功能缺陷，两个契约项独立满足。T11 未触碰这条（不在回归清单范围，UI 微调非"BROKEN"）。
- T7：`App._resonanceText` 的" · 周天"后缀原先被 `labels.length` 挡住——同一 wuxing 不重复出现时（真实周天场景）后缀渲染不出来，M4 遗留的不可达分支，T7 验收时发现即修，4 个分支（无 labels / labels-only / labels+周天 / 全分支）复核字节相同（除新增分支外）。4 条陈旧"暂缺"注释同步改写。
- T8：报告口径有一处算术失误——"13 个技能里 11 个 manaCost 为 0"是错的，grep 实数是 8 个（5 个战术档技能各 30 蓝，其余 8 个为 0）。T11 复核 settings.js 13 个新技能块，manaCost 分布确认 8/13 为 0、5/13 为 30，与裁定4 的名单精确对应。
- T9：`Stand To Roll.fbx` 在会话开始时的 git status 快照显示为未跟踪文件——实为陈旧快照，该文件已在 1b849a3 提交入库，T9 的"待处理"事项作废。T11 会话开始时的 git status 同样干净（无该文件的未跟踪条目），佐证 T9 判断正确。
- T9 DISCLOSED COVERAGE GAP：两套 headless 套件都不做基于文件系统的资产存在性检查（`npm run check` 绿不代表贴图路径真的换对了）——T9 记在案，指名候选进 T11 回归清单。T11 采用的是运行时网络请求核验（`read_network_requests`，浏览器真实加载全链路 200/304，零 404），不是新增 headless 断言；这条覆盖缺口本身仍然成立，留给日后若需要 CI 级资产校验时再补。
- T10：三轮 og.png 出图，前两轮agent 自拍（round 1 黑背景裁到瓶子、round 2 竞技场对但人群缺席）均不合格，第三轮由 controller 直接摆拍定稿（bdb7842：施法者张臂 pose + 业火燎原 + 双边界光弧 + 石碑环 + 满编人群，1200×630 PNG8 128KB）。摆拍过程中发现真实渲染 bug（见下）。

**摆拍中定位的真实 bug（27b8397，T10）**：`EnemyRenderer` 的敌群实例网格与预警贴花缺 `frustumCulled=false`——实例变换只写在 GPU 属性缓冲区里，几何体自身的包围球从未扩张，默认剔除测试拿的是原点附近约 1 米的胶囊体；镜头一旦不看向 (0,0)，整个敌群（包括不在视锥内应该照常存在的怪）静默消失。战斗在场地边缘打，敌人其实还在场上咬人，只是渲染不出来。仓库里其它世界空间对象池早都遵循这条"必须显式关闭视锥剔除"的约定（`ProceduralGeometry` 注释里也提过），唯独敌群自己漏了。修复后现场复核：镜头远离出生点时 10/10 敌人正常渲染于 (-24,-28) 一类的边缘坐标。T11 未复现相关问题（本轮浏览器回归多次将镜头/敌人拉到场地边缘，含地心引战全链路测试，未见消失）。

**T-later 记录**（T12 遗留，评审确认方向安全，未接线）：`AimController` 的预瞄指示器在弑神一闪 Lv3+ 仍然按未缩放的原始 8m 预览冲程（低估实际可达距离）——控制器本身没有 loadout 访问权限，纯打磨项，不影响正确性（真实位移走 `scaledDashRange`，只有预瞄的视觉提示滞后）。T11 未接线（不在回归清单范围内，机制本身无误）。

**保留语义**（非缺陷，记录设计意图/当前状态）：
- T1 sandbox-only 英文 toast：编辑器沙盒模式下的角色切换 toast 仍是纯英文（run 模式已走 `t()`），记在案留给未来双语补齐，非本里程碑范围。
- reduceFlashes 现在是字面 spec 值：`FLASH_DAMP=0.2`（−80%），T1 从 M5 时代的 ×0.5 仓库惯例改到位，T11 用真实 `trigger()` 调用复核过（见上）。
- 护盾优先级：吸收池先于 HP 扣（不触发无敌帧），穿盾余量才进 HP 触发 iframes——T11 用真实 `takeDamage()` 两段式调用复核（部分吸收 25/40 不动 HP；追加吸收使总量溢出 10 点精确进 HP）。

**T11 本任务验收**：
- 三套件：`npm run check` 11/11 clips 绿（classic 5 + sorcerer 6，含 roll）；`npm run check:game` 63/63 blocks 绿；`npm run sim` 新手 0% / 基线 59%（死亡中位 14.0min）/ 熟练 100% / 高手 100%，等级中位 29——与 M5 收官时的数字逐位相同，13 个新技能没有移动 sim 的抽象模型，带内直接过。
- spec §9.5 十项回归 + M6 六项增补：浏览器内 pump 模式真实交互逐项过（新档开局/能死/能赢/能升级/能融合/双角色/双语/减闪/性能模式/刷新恢复 + 13 技能逐个放/护盾吸收/法力门拒放/土全链 mark→detonate→quench→天崩/光环装座即常驻卸座即消失/资产零 404），细节数字见 task-11-report.md。
- 土全链数字与 T7 验收记录逐位吻合：岩刺 120 / 剑雨引爆 260（基础 104 + 反应 1.5×=156）/ 淬炼旗标 true→（金系消费）→false / 土禁咒 40 场伤 + 眩晕 + 击退 + 充能清零。
- 未发现需要修复的真实缺陷。回归中段一度怀疑生命绽放（lifebloom）的 `healPlayer` 在连续快速重放下失效（`_detonated` 状态复现两次为 false）——追加隔离测试证明是测试手法本身的伪影（脚本用 `cooldowns.set(...,0)` 连续快速重放同一技能，超出真实玩家 3.2s 冷却能达到的调用密度），干净场景下单次施放 heal/damage 均精确命中settings 数值，未列为缺陷。
- git tag：本任务按 controller 显式指令跳过（见裁定7 落地实况），留给里程碑终审。
