# M2 成长循环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 M1 灰盒变成真肉鸽循环：开局一个技能 → 升级三选一（新主动/技能升级/通用被动）→ 修正层驱动变强 → 弃权保底/里程碑保新卡/时来运转刷新 → 施法回响。世界在选卡时全停。

**Architecture:** 三个新纯逻辑模块（Modifiers 修正层 / Loadout 席位 / UpgradePool 卡池）+ 一个 DOM 覆盖层（UpgradeUi），全部按 M1 已落地的真实接口对接：`settings.run.loadout` 六席 [LMB, RMB, Q, E, R, T]、`App._quickCast(element)`、`RunManager.tick` 内的 `pickups.tick` 返回值（目前被丢弃，M2 接住它）、`CombatSystem` 六处伤害读数、`abilities.onRetire` 钩子。修正层铁律：**升级只写修正层，settings 基值永不被污染**（spec §2）。

**Tech Stack:** 同 M1——Three.js、Vite、原生 ESM、`scripts/check-game.mjs` 聚合断言。

## Global Constraints（每任务默认遵守，含 M1 勘误三规程）

- 修正层铁律：生效值 = settings 基值 × Modifiers；任何升级不得写 settings（spec §2 防污染）
- run 暂停 = 全停：选卡期间走位/施法/VFX/敌人全部冻结（spec 两种暂停）
- 固定 60Hz tick 体系不变；热路径零分配；沙盒（无 `#run`）行为逐帧不变
- 所有数值进 settings 且**同一任务内必须有读它的代码**（勘误规程 ③）
- 接口块中字段与方法不共名；每步落笔前对照真实文件核对锚点与时序（勘误规程 ①）
- 每个新公开方法至少一条会失败的断言（勘误规程 ②）
- commit 用仓库自然句式，结尾 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: Modifiers 修正层

**Files:**
- Create: `src/run/Modifiers.js`
- Modify: `scripts/check-game.mjs`（在 stress 块之前追加断言块）

**Interfaces:**
- Produces:

```
export const PASSIVES = {
  swift:     { name: '疾行',   max: 3 },   // +8% 移速/级
  vitality:  { name: '淬体',   max: 3 },   // +10% 最大生命/级
  focus:     { name: '凝神',   max: 3 },   // 冷却 ×0.94/级，全局下限 0.6（spec CD 规则③）
  scavenger: { name: '拾荒',   max: 3 },   // +10% 经验/级
  echo:      { name: '施法回响', max: 3 }, // 4% 双发概率/级（满级 12%，spec）
  reroll:    { name: '时来运转', max: 3 }  // 每次三选一可刷新次数 = 等级
};

class Modifiers {
  reset(): void
  bumpDamage(element): void            // 技能每级 +25%（spec 锚4）
  bumpPassive(id): boolean             // false when already at max
  passiveLevel(id): number
  damageMult(element): number          // 1 + 0.25 × (skill level - 1) 的存放地
  moveSpeedMult(): number
  maxHpMult(): number
  xpMult(): number
  cooldownMult(): number               // max(0.6, 0.94^focusLevel)
  echoChance(): number
}
```

数值常量（0.25 / 0.08 / 0.10 / 0.94 / 0.6 / 0.10 / 0.04）进 `settings.upgrades` 块（本任务新增并**在实现里读取**）。

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加（import 区加一行）：
import { Modifiers, PASSIVES } from '../src/run/Modifiers.js';

/* ---- modifiers: upgrades never touch settings, multipliers stack right ---- */
{
  const mods = new Modifiers();
  assert.equal(mods.damageMult('ice'), 1, 'mods: fresh run multiplies by 1');
  mods.bumpDamage('ice');
  mods.bumpDamage('ice');
  assert.ok(Math.abs(mods.damageMult('ice') - 1.5) < 1e-9, 'mods: two bumps = +50%');
  assert.equal(mods.damageMult('thunder'), 1, 'mods: per-element isolation');

  assert.ok(mods.bumpPassive('focus'));
  assert.ok(Math.abs(mods.cooldownMult() - 0.94) < 1e-9, 'mods: one focus level = ×0.94');
  mods.bumpPassive('focus');
  mods.bumpPassive('focus');
  assert.ok(!mods.bumpPassive('focus'), 'mods: passive refuses past its max');
  assert.ok(mods.cooldownMult() >= 0.6, 'mods: CD floor 0.6 holds (spec cap 40%)');

  mods.bumpPassive('echo');
  assert.ok(Math.abs(mods.echoChance() - settings.upgrades.echoPerLevel) < 1e-9);
  mods.bumpPassive('vitality');
  assert.ok(mods.maxHpMult() > 1 && mods.moveSpeedMult() === 1, 'mods: keys independent');
  assert.equal(mods.passiveLevel('reroll'), 0);

  mods.reset();
  assert.equal(mods.damageMult('ice'), 1, 'mods: reset wipes everything');
  assert.equal(mods.passiveLevel('focus'), 0);
  assert.ok(PASSIVES.swift.max === 3 && PASSIVES.reroll.name.length > 0);
  console.log('ok  modifiers');
}
```

- [ ] **Step 2: 确认失败** — `npm run check:game` → module not found

- [ ] **Step 3: 实现（settings 先行）**

`settings.js` 在 `run:` 块后插入：

```js
  /**
   * The growth loop's own numbers (spec §4.5 / anchors 4). The modifier layer
   * multiplies these onto the base values above — upgrades never write into
   * any other settings block, so the sandbox and the next run stay clean.
   */
  upgrades: {
    damagePerLevel: 0.25, // each skill level past 1 adds this to the multiplier
    swiftPerLevel: 0.08,
    vitalityPerLevel: 0.1,
    focusPerLevel: 0.94, // multiplicative CD factor per focus level...
    cooldownFloor: 0.6, // ...never past the spec's 40% total reduction cap
    scavengerPerLevel: 0.1,
    echoPerLevel: 0.04, // 12% at max, spec's number
    skillLevelMax: 5,
    passiveWeights: { upgrade: 3, newActive: 2, passive: 1 }, // draw weights
    milestones: [5, 10, 15], // levels that guarantee a new-active card
    skipHeal: 0.1 // declining all three cards heals this fraction of max hp
  },
```

```js
// src/run/Modifiers.js
import { settings } from '../config/settings.js';

/**
 * The run's temporary strength (spec §2 "数值分层").
 *
 * Effective value = settings base × whatever lives here. Upgrades only ever
 * write to this object and the whole thing is discarded when the run ends, so
 * the editor's sliders — the game's factory numbers — never get polluted by
 * one lucky build.
 */
export const PASSIVES = {
  swift: { name: '疾行', max: 3 },
  vitality: { name: '淬体', max: 3 },
  focus: { name: '凝神', max: 3 },
  scavenger: { name: '拾荒', max: 3 },
  echo: { name: '施法回响', max: 3 },
  reroll: { name: '时来运转', max: 3 }
};

export class Modifiers {
  constructor() {
    this.reset();
  }

  reset() {
    this._damage = Object.create(null); // element -> added multiplier
    this._passives = Object.create(null); // id -> level
  }

  bumpDamage(element) {
    this._damage[element] = (this._damage[element] ?? 0) + settings.upgrades.damagePerLevel;
  }

  bumpPassive(id) {
    const level = this._passives[id] ?? 0;
    if (level >= PASSIVES[id].max) return false;
    this._passives[id] = level + 1;
    return true;
  }

  passiveLevel(id) {
    return this._passives[id] ?? 0;
  }

  damageMult(element) {
    return 1 + (this._damage[element] ?? 0);
  }

  moveSpeedMult() {
    return 1 + settings.upgrades.swiftPerLevel * this.passiveLevel('swift');
  }

  maxHpMult() {
    return 1 + settings.upgrades.vitalityPerLevel * this.passiveLevel('vitality');
  }

  xpMult() {
    return 1 + settings.upgrades.scavengerPerLevel * this.passiveLevel('scavenger');
  }

  cooldownMult() {
    return Math.max(
      settings.upgrades.cooldownFloor,
      Math.pow(settings.upgrades.focusPerLevel, this.passiveLevel('focus'))
    );
  }

  echoChance() {
    return settings.upgrades.echoPerLevel * this.passiveLevel('echo');
  }
}
```

- [ ] **Step 4: 跑通** — `npm run check:game` 全绿

- [ ] **Step 5: Commit**

```bash
git add src/run/Modifiers.js src/config/settings.js scripts/check-game.mjs
git commit -m "Layer the run's strength over the base numbers instead of into them"
```

---

### Task 2: Loadout 席位

**Files:**
- Create: `src/run/Loadout.js`
- Modify: `src/config/settings.js`（run 块加 `draftLoadout: true`）
- Modify: `src/ui/Editor.js`（`_buildRun` 加 draft 开关）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Consumes: `settings.run.loadout`（六席数组，M1 已有）、`settings.run.draftLoadout`（本任务新增）、`settings.upgrades.skillLevelMax`
- Produces:

```
class Loadout {
  seats: (string|null)[]           // length 6, slot order = [LMB, RMB, Q, E, R, T]
  reset(): void                    // draftLoadout ? 只保留 seats[0] : 整份拷贝 settings.run.loadout
  elementAt(seat): string|null
  equippedList(): string[]         // 非空席位的元素，席位序
  hasEmpty(): boolean
  has(element): boolean
  acquire(element): number         // 填入最靠前空席，返回席位号；无空席/已持有返回 -1
  levelOf(element): number         // 未持有 0，持有 1..skillLevelMax
  upgrade(element): boolean        // 到上限返回 false
  isMaxed(element): boolean
}
```

（字段 `seats` 与方法名无冲突——勘误规程 ①。）

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
import { Loadout } from '../src/run/Loadout.js';

/* ---- loadout: draft starts with one seat, acquire fills forward ---- */
{
  const saved = settings.run.draftLoadout;
  settings.run.draftLoadout = true;
  const loadout = new Loadout();
  loadout.reset();
  assert.equal(loadout.elementAt(0), settings.run.loadout[0], 'loadout: draft keeps seat 0');
  assert.equal(loadout.equippedList().length, 1, 'loadout: draft empties the rest');
  assert.ok(loadout.hasEmpty());
  assert.equal(loadout.levelOf(settings.run.loadout[0]), 1, 'loadout: starter is level 1');

  assert.equal(loadout.acquire('snare'), 1, 'loadout: acquire fills the first gap');
  assert.equal(loadout.acquire('snare'), -1, 'loadout: no duplicates');
  assert.ok(loadout.has('snare') && loadout.levelOf('snare') === 1);

  for (let n = 0; n < settings.upgrades.skillLevelMax - 1; n++) {
    assert.ok(loadout.upgrade('snare'), `loadout: upgrade ${n + 2} accepted`);
  }
  assert.ok(!loadout.upgrade('snare'), 'loadout: cap at skillLevelMax');
  assert.ok(loadout.isMaxed('snare'));

  settings.run.draftLoadout = false;
  loadout.reset();
  assert.equal(loadout.equippedList().length, 6, 'loadout: full mode copies all six');
  assert.ok(!loadout.hasEmpty());
  settings.run.draftLoadout = saved;
  console.log('ok  loadout');
}
```

- [ ] **Step 2: 确认失败** — module not found

- [ ] **Step 3: 实现**

```js
// src/run/Loadout.js
import { settings } from '../config/settings.js';

/**
 * Who is on stage this run (spec §6).
 *
 * Six seats in key order [LMB, RMB, Q, E, R, T]. A drafted run starts with
 * seat 0 only — the editor's Run dropdowns stay the debug override via
 * `draftLoadout: false`, which copies the whole configured six for testing.
 * Seats are run state, never written back into settings.
 */
export class Loadout {
  constructor() {
    this.seats = [null, null, null, null, null, null];
    this._levels = Object.create(null);
  }

  reset() {
    const configured = settings.run.loadout;
    this._levels = Object.create(null);
    for (let seat = 0; seat < 6; seat++) {
      const keep = settings.run.draftLoadout ? seat === 0 : true;
      this.seats[seat] = keep ? configured[seat] : null;
      if (this.seats[seat]) this._levels[this.seats[seat]] = 1;
    }
  }

  elementAt(seat) {
    return this.seats[seat] ?? null;
  }

  equippedList() {
    return this.seats.filter(Boolean);
  }

  hasEmpty() {
    return this.seats.includes(null);
  }

  has(element) {
    return this.seats.includes(element);
  }

  acquire(element) {
    if (this.has(element)) return -1;
    const seat = this.seats.indexOf(null);
    if (seat === -1) return -1;
    this.seats[seat] = element;
    this._levels[element] = 1;
    return seat;
  }

  levelOf(element) {
    return this._levels[element] ?? 0;
  }

  upgrade(element) {
    const level = this.levelOf(element);
    if (level === 0 || level >= settings.upgrades.skillLevelMax) return false;
    this._levels[element] = level + 1;
    return true;
  }

  isMaxed(element) {
    return this.levelOf(element) >= settings.upgrades.skillLevelMax;
  }
}
```

`settings.run` 的 `loadout:` 行前加：

```js
    draftLoadout: true, // run starts with seat 0 only; false = all six (debug)
```

`Editor._buildRun` 的 spawnBase 行前加：

```js
    folder.add(r, 'draftLoadout').name('draft loadout');
```

- [ ] **Step 4: 跑通** — `npm run check:game` 全绿

- [ ] **Step 5: Commit**

```bash
git add src/run/Loadout.js src/config/settings.js src/ui/Editor.js scripts/check-game.mjs
git commit -m "Seat the loadout as run state: draft one, earn the rest"
```

---

### Task 3: UpgradePool 卡池

**Files:**
- Create: `src/run/UpgradePool.js`
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Consumes: `Loadout`、`Modifiers`、`PASSIVES`、`createRng` 实例、`settings.upgrades`、`ELEMENTS`
- Produces:

```
class UpgradePool {
  constructor(rng, loadout, modifiers)
  draw(level): Card[]   // 至多 3 张互不重复；level ∈ milestones 时若可能必含 'new'
}
Card = {
  kind: 'upgrade' | 'new' | 'passive',
  element?: string,     // upgrade/new
  passive?: string,     // passive 的 PASSIVES 键
  title: string,        // 中文卡名（元素名沿用 ELEMENT_META 的 name 由 UI 层取；此处存 element/passive 键 + 动词）
  body: string          // 一句效果
}
```

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
import { UpgradePool } from '../src/run/UpgradePool.js';

/* ---- upgrade pool: deterministic, distinct, weighted, milestone-aware ---- */
{
  const saved = settings.run.draftLoadout;
  settings.run.draftLoadout = true;
  const make = (seed) => {
    const loadout = new Loadout();
    loadout.reset();
    const mods = new Modifiers();
    return { pool: new UpgradePool(createRng(seed), loadout, mods), loadout, mods };
  };

  // Same seed, same cards — the daily-seed contract reaches the card row.
  const a = make(11).pool.draw(2);
  const b = make(11).pool.draw(2);
  assert.deepEqual(a.map((c) => c.kind + (c.element ?? c.passive)),
    b.map((c) => c.kind + (c.element ?? c.passive)), 'pool: seeded draws replay');

  // Distinct cards, at most three, every card actionable.
  const { pool, loadout } = make(23);
  for (let round = 0; round < 50; round++) {
    const cards = pool.draw(2);
    assert.ok(cards.length >= 1 && cards.length <= 3);
    const keys = cards.map((c) => c.kind + (c.element ?? c.passive));
    assert.equal(new Set(keys).size, keys.length, 'pool: no duplicate cards in a hand');
    for (const card of cards) {
      if (card.kind === 'upgrade') assert.ok(!loadout.isMaxed(card.element));
      if (card.kind === 'new') assert.ok(!loadout.has(card.element) && loadout.hasEmpty());
      assert.ok(card.title.length > 0 && card.body.length > 0, 'pool: cards carry copy');
    }
  }

  // Milestone levels guarantee a new-active card while seats remain.
  const m = make(31);
  const milestone = m.pool.draw(settings.upgrades.milestones[0]);
  assert.ok(milestone.some((c) => c.kind === 'new'), 'pool: milestone forces a new active');

  // Exhaustion: everything maxed and seated → empty hand (skip-heal path).
  const x = make(41);
  for (const element of ELEMENTS) x.loadout.acquire(element);
  for (const element of x.loadout.equippedList()) {
    while (!x.loadout.isMaxed(element)) x.loadout.upgrade(element);
  }
  for (const id of Object.keys(PASSIVES)) {
    while (x.mods.bumpPassive(id)) { /* to max */ }
  }
  assert.equal(x.pool.draw(20).length, 0, 'pool: a full build draws nothing');
  settings.run.draftLoadout = saved;
  console.log('ok  upgrade pool');
}
```

- [ ] **Step 2: 确认失败** — module not found

- [ ] **Step 3: 实现**

```js
// src/run/UpgradePool.js
import { settings, ELEMENTS } from '../config/settings.js';
import { PASSIVES } from './Modifiers.js';

/**
 * What the level-up can offer (spec §6 卡池).
 *
 * Candidates are generated fresh from the live loadout + modifiers every draw:
 * upgrades for seated skills that aren't maxed, new actives while a seat is
 * open, generic passives below their caps. Weighted sampling without
 * replacement gives up to three distinct cards; milestone levels bump a new
 * active into the hand while one exists, and a completely full build simply
 * draws nothing — the UI turns that into the skip-heal.
 */
const UPGRADE_BODY = '伤害 +25%';

export class UpgradePool {
  constructor(rng, loadout, modifiers) {
    this.rng = rng;
    this.loadout = loadout;
    this.modifiers = modifiers;
  }

  draw(level) {
    const w = settings.upgrades.passiveWeights;
    const candidates = [];

    for (const element of this.loadout.equippedList()) {
      if (this.loadout.isMaxed(element)) continue;
      candidates.push({
        weight: w.upgrade,
        card: {
          kind: 'upgrade',
          element,
          title: `升级 · Lv${this.loadout.levelOf(element) + 1}`,
          body: UPGRADE_BODY
        }
      });
    }

    if (this.loadout.hasEmpty()) {
      for (const element of ELEMENTS) {
        if (this.loadout.has(element)) continue;
        candidates.push({
          weight: w.newActive,
          card: { kind: 'new', element, title: '新技能', body: '进入下一个空位' }
        });
      }
    }

    for (const [id, meta] of Object.entries(PASSIVES)) {
      if (this.modifiers.passiveLevel(id) >= meta.max) continue;
      candidates.push({
        weight: w.passive,
        card: {
          kind: 'passive',
          passive: id,
          title: meta.name,
          body: `${meta.name} Lv${this.modifiers.passiveLevel(id) + 1}`
        }
      });
    }

    const hand = [];
    const wantNew =
      settings.upgrades.milestones.includes(level) &&
      candidates.some((c) => c.card.kind === 'new');
    if (wantNew) {
      hand.push(this._take(candidates.filter((c) => c.card.kind === 'new'), candidates));
    }
    while (hand.length < 3 && candidates.length > 0) {
      hand.push(this._take(candidates, candidates));
    }
    return hand;
  }

  /** Weighted pick from `from`, removed from `all` so a hand never repeats. */
  _take(from, all) {
    let total = 0;
    for (const c of from) total += c.weight;
    let roll = this.rng() * total;
    let chosen = from[from.length - 1];
    for (const c of from) {
      roll -= c.weight;
      if (roll <= 0) {
        chosen = c;
        break;
      }
    }
    all.splice(all.indexOf(chosen), 1);
    return chosen.card;
  }
}
```

- [ ] **Step 4: 跑通** — `npm run check:game` 全绿

- [ ] **Step 5: Commit**

```bash
git add src/run/UpgradePool.js scripts/check-game.mjs
git commit -m "Deal the level-up hand: upgrades, new actives, passives, milestones"
```

---

### Task 4: 修正层接入效果端（伤害 / 经验 / 移速）

**Files:**
- Modify: `src/run/CombatSystem.js`（构造器 + 六处伤害读数）
- Modify: `src/run/PickupSystem.js`（xp 入账一处）
- Modify: `src/animation/CharacterController.js`（`move` 加 scale 参数，默认 1——沙盒零变化）
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Produces: `CombatSystem` 构造器变为 `constructor(targets, modifiers = null)`，内部 `this.mods`；`PickupSystem` 新增可赋值字段 `mods`（默认 null）；`CharacterController.move(direction, dt, speedScale = 1)`。

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 追加：
/* ---- modifier plumbing: damage and xp actually scale ---- */
{
  const hits = [];
  const fakeTargets = {
    damageOnce: (id, p, r, amount) => (hits.push(amount), 1),
    damage: (p, r, amount) => (hits.push(amount), 1),
    slow: () => {}
  };
  const mods = new Modifiers();
  mods.bumpDamage('ice'); // ice ×1.25
  const combat = new CombatSystem(fakeTargets, mods);
  const ice = {
    element: 'ice', phase: 'travel', age: 0.2, impactTime: 0, fadeTime: 0,
    position: { x: 1, z: 0 }, origin: { x: 0, z: 0 },
    direction: { x: 1, z: 0 }, length: 8, u: 0.2
  };
  combat.tick(1 / 60, [ice]);
  const base = settings.combat.ice.damage;
  assert.ok(hits.some((amount) => Math.abs(amount - base * 1.25) < 1e-6),
    'combat: sweep damage rides the modifier');

  const pickups = new PickupSystem();
  const scavenger = new Modifiers();
  scavenger.bumpPassive('scavenger');
  pickups.mods = scavenger;
  pickups.dropAt(0.1, 0, 0);
  pickups.tick(1 / 60, { x: 0, z: 0 });
  assert.ok(Math.abs(pickups.xp - settings.run.gemBase * 1.1) < 1e-6,
    'pickups: xp rides the scavenger multiplier');
  console.log('ok  modifier plumbing');
}
```

（`CombatSystem` 现有的 combat-shapes 断言块构造处 `new CombatSystem(fakeTargets)` 无第二参——默认 null 不破坏，跑一遍确认。`move` 的 scale 无法在 Node 便捷验证——浏览器集成时验证，见 Task 7 Step 5。）

- [ ] **Step 2: 确认失败** — `npm run check:game` → 构造器第二参未生效/`pickups.mods` 无效果导致断言失败

- [ ] **Step 3: 实现**

`CombatSystem.js`：

```js
  constructor(targets, modifiers = null) {
    this.targets = targets;
    /** Damage multipliers from the run's upgrade layer; null in the sandbox. */
    this.mods = modifiers;
```

新增私有助手（放在 `_castKey` 之前）：

```js
  _amp(element) {
    return this.mods ? this.mods.damageMult(element) : 1;
  }
```

六处读数逐一乘上（勘误规程：写完对照文件核对行号）：
- sweep：`c.damage` → `c.damage * this._amp(ability.element)`
- burst 引爆：`c.damage` → `c.damage * this._amp(ability.element)`
- burst 灼烧：`this._dot(castId, step, c.burnDps)` → `this._dot(castId, step, c.burnDps * this._amp(ability.element))`
- lineTick：`const perSecond = c.dps / LINE_SAMPLES;` → `const perSecond = (c.dps * this._amp(ability.element)) / LINE_SAMPLES;`
- zoneTick：`c.dps * step` → `c.dps * this._amp(ability.element) * step`

`PickupSystem.js`：constructor 里加 `this.mods = null;`（注释：`// Run upgrade layer; null in the sandbox — xp then passes through unscaled.`）；入账行改：

```js
        this.xp += this.value[i] * (this.mods ? this.mods.xpMult() : 1);
```

`CharacterController.js` 的 `move`：

```js
  move(direction, dt, speedScale = 1) {
    const c = settings.character;
    const throttle = Math.min(1, Math.hypot(direction.x, direction.z));

    this._desiredVelocity
      .set(direction.x, 0, direction.z)
      .multiplyScalar(c.walkSpeed * speedScale);
```

（其余行不动；`isMoving`/`_blendLegs` 读 velocity 自动跟随，腿速比走 `runPlayback` 原逻辑。）

- [ ] **Step 4: 跑通** — `npm run check:game` 全绿（含既有 combat-shapes 块）

- [ ] **Step 5: Commit**

```bash
git add src/run/CombatSystem.js src/run/PickupSystem.js src/animation/CharacterController.js scripts/check-game.mjs
git commit -m "Let the upgrade layer reach damage, xp and stride"
```

---

### Task 5: RunManager 接住升级数

**Files:**
- Modify: `src/run/RunManager.js`
- Modify: `scripts/check-game.mjs`

**Interfaces:**
- Produces: `RunManager.pendingLevels: number`——`tick` 内累加 `pickups.tick` 的返回值（目前该返回值被丢弃）；`start()` 归零；App 消费后自行递减。

- [ ] **Step 1: 断言（先失败）**

```js
// scripts/check-game.mjs 的 run-manager 块内、`console.log('ok  run manager')` 之前追加：
  // Level-ups queue on the manager instead of vanishing.
  run.start();
  run.tick(1 / 60, { x: 0, z: 0 });
  pickups.xp = pickups.xpNeed(1) + pickups.xpNeed(2) + 1; // enough for two levels
  run.tick(1 / 60, { x: 0, z: 0 });
  assert.ok(run.pendingLevels >= 2, `run: level-ups queue (got ${run.pendingLevels})`);
  run.start();
  assert.equal(run.pendingLevels, 0, 'run: restart clears the queue');
```

- [ ] **Step 2: 确认失败** — `pendingLevels` undefined

- [ ] **Step 3: 实现**

constructor 加 `this.pendingLevels = 0;`；`start()` 加 `this.pendingLevels = 0;`；tick 内：

```js
    this.pendingLevels += this.s.pickups.tick(step, playerPos);
```

（替换原来丢弃返回值的 `this.s.pickups.tick(step, playerPos);` 一行。）

- [ ] **Step 4: 跑通** — `npm run check:game` 全绿

- [ ] **Step 5: Commit**

```bash
git add src/run/RunManager.js scripts/check-game.mjs
git commit -m "Queue earned level-ups on the manager instead of dropping them"
```

---

### Task 6: UpgradeUi 三选一覆盖层

**Files:**
- Create: `src/run/UpgradeUi.js`
- Modify: `src/ui/styles.css`（追加 `.upgrade-ui` 段）

**Interfaces:**
- Produces:

```
class UpgradeUi {
  constructor(parent = document.body)
  open(cards: Card[], context: { rerolls: number, summary: string }): void
  onChoice: (result) => void   // result ∈ {action:'pick', card} | {action:'skip'} | {action:'reroll'}
  readonly isOpen: boolean
  close(): void
  dispose(): void
}
```

键盘：自己的 window `keydown` **capture** 监听，仅在 `isOpen` 时拦截（`stopPropagation` + `preventDefault`），`1/2/3` 选卡、`4` 或点击按钮弃权——游戏侧 InputManager 因此收不到任何按键（全停的输入面）。空手（cards 为空）时直接渲染"弃权回血"单按钮。

- [ ] **Step 1: 实现**

```js
// src/run/UpgradeUi.js
import { ELEMENT_META } from '../config/settings.js';

/**
 * The level-up hand (spec §6): world frozen behind a dim, three cards, keys
 * 1/2/3 or click, skip heals, reroll while 时来运转 has charges. Grey-box
 * styling — the gilded card art belongs to M5.
 *
 * Owns its keydown in the capture phase so the game's InputManager never sees
 * a keystroke while a hand is open; that plus App's freeze gate is the whole
 * "run pause is a full stop" contract.
 */
export class UpgradeUi {
  constructor(parent = document.body) {
    this.root = document.createElement('div');
    this.root.className = 'upgrade-ui';
    this.root.style.display = 'none';
    parent.appendChild(this.root);
    this.onChoice = null;
    this._cards = [];
    this._open = false;
    window.addEventListener('keydown', this._onKey, true);
  }

  get isOpen() {
    return this._open;
  }

  open(cards, { rerolls = 0, summary = '' } = {}) {
    this._cards = cards;
    this._open = true;
    const body = cards
      .map((card, i) => {
        const element = card.element ? (ELEMENT_META[card.element]?.name ?? card.element) : '';
        return (
          `<button class="upgrade-ui__card" data-i="${i}">` +
          `<b>${i + 1}</b><em>${element ? element + ' · ' : ''}${card.title}</em>` +
          `<span>${card.body}</span></button>`
        );
      })
      .join('');
    this.root.innerHTML =
      `<div class="upgrade-ui__dim"></div><div class="upgrade-ui__panel">` +
      `<p class="upgrade-ui__head">${cards.length ? '升级！选择其一' : '无可选项'}</p>` +
      `<div class="upgrade-ui__hand">${body}</div>` +
      `<div class="upgrade-ui__row">` +
      `<button class="upgrade-ui__minor" data-act="skip">弃权 · 回复生命 (4)</button>` +
      (rerolls > 0
        ? `<button class="upgrade-ui__minor" data-act="reroll">时来运转 · 刷新 ×${rerolls}</button>`
        : '') +
      `</div><p class="upgrade-ui__summary">${summary}</p></div>`;
    this.root.style.display = '';
    this.root.querySelectorAll('[data-i]').forEach((el) =>
      el.addEventListener('click', () => this._pick(Number(el.dataset.i)))
    );
    this.root.querySelector('[data-act="skip"]').addEventListener('click', () =>
      this._emit({ action: 'skip' })
    );
    this.root.querySelector('[data-act="reroll"]')?.addEventListener('click', () =>
      this._emit({ action: 'reroll' })
    );
  }

  close() {
    this._open = false;
    this.root.style.display = 'none';
    this.root.innerHTML = '';
  }

  _pick(i) {
    if (this._cards[i]) this._emit({ action: 'pick', card: this._cards[i] });
  }

  _emit(result) {
    this.close();
    this.onChoice?.(result);
  }

  _onKey = (event) => {
    if (!this._open) return;
    event.stopPropagation();
    if (event.key >= '1' && event.key <= '3') this._pick(Number(event.key) - 1);
    else if (event.key === '4') this._emit({ action: 'skip' });
    else return;
    event.preventDefault();
  };

  dispose() {
    window.removeEventListener('keydown', this._onKey, true);
    this.root.remove();
  }
}
```

（`ELEMENT_META` 是 settings.js 既有导出——写码前 `grep -n "ELEMENT_META" src/config/settings.js` 核对其 `name` 字段；若字段名不同按实调整。）

- [ ] **Step 2: 样式**

`styles.css` 追加：

```css
/* Level-up hand — grey-box; the real card art is milestone 5. */
.upgrade-ui__dim {
  position: fixed;
  inset: 0;
  background: rgba(3, 6, 10, 0.72);
  z-index: 40;
}
.upgrade-ui__panel {
  position: fixed;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 14px;
  z-index: 41;
  color: #e6edf5;
  font-family: system-ui, -apple-system, 'PingFang SC', 'Microsoft YaHei', sans-serif;
}
.upgrade-ui__head { font-size: 18px; letter-spacing: 0.2em; }
.upgrade-ui__hand { display: flex; gap: 14px; }
.upgrade-ui__card {
  width: 168px;
  min-height: 150px;
  padding: 14px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  background: rgba(16, 24, 36, 0.92);
  border: 1px solid rgba(120, 160, 210, 0.35);
  border-radius: 10px;
  color: inherit;
  cursor: pointer;
  text-align: left;
}
.upgrade-ui__card:hover { border-color: rgba(190, 220, 255, 0.8); }
.upgrade-ui__card b { opacity: 0.5; }
.upgrade-ui__card em { font-style: normal; font-weight: 700; }
.upgrade-ui__card span { font-size: 12px; opacity: 0.75; }
.upgrade-ui__row { display: flex; gap: 10px; }
.upgrade-ui__minor {
  padding: 6px 14px;
  background: rgba(16, 24, 36, 0.8);
  border: 1px solid rgba(120, 160, 210, 0.25);
  border-radius: 8px;
  color: inherit;
  cursor: pointer;
  font-size: 12px;
}
.upgrade-ui__summary { font-size: 12px; opacity: 0.6; }
```

- [ ] **Step 3: Commit**（浏览器行为随 Task 7 一并验证）

```bash
git add src/run/UpgradeUi.js src/ui/styles.css
git commit -m "Lay out the level-up hand: three cards, skip, reroll"
```

---

### Task 7: App 集成（全停选卡 · 应用卡 · 回响 · 席位键位 · 冷却修正）

**Files:**
- Modify: `src/core/App.js`
- Modify: `src/run/RunHud.js`（verdict 文案沿用；无接口变化则不动）

**改动清单**（每处先 grep 锚点核对——勘误规程 ①）：

- [ ] **Step 1: 构造 run 侧新对象**

App constructor 的 run 块里（`new PickupSystem()` 附近，锚点 `this.playerState = new PlayerState();`）：

```js
      this.modifiers = new Modifiers();
      this.loadout = new Loadout();
      this.upgradePool = new UpgradePool(rng, this.loadout, this.modifiers);
      this.upgradeUi = new UpgradeUi();
      this.upgradeUi.onChoice = (result) => this._onUpgradeChoice(result);
      this.pickups.mods = this.modifiers;
```

`CombatSystem` 构造改为 `new CombatSystem(this.targets, this.modifiers)`（锚点：现有 `this.combat = new CombatSystem(this.targets)`）。`this.run.start()` 前插 `this.loadout.reset(); this.modifiers.reset();`。

- [ ] **Step 2: 席位改读 Loadout + HUD 标签可刷新**

现有两处 `settings.run.loadout[seat]`（`pointer:confirm` 的 seat 0、`_handleAction` 的 seat 映射与 RMB seat 1）全部改为 `this.loadout.elementAt(seat)`。把构造期的"HUD 标签+置灰"块（锚点 `settings.run.loadout.forEach((element, slot) => ...)`）提取成方法：

```js
  _syncRunHudLabels() {
    const labels = {};
    ELEMENTS.forEach((element) => (labels[element] = null)); // dim by default
    this.loadout.seats.forEach((element, seat) => {
      if (element) labels[element] = RUN_SLOT_KEYS[seat];
    });
    this.hud.setRunLabels?.(labels) ?? this._applyRunLabels(labels);
  }
```

（实际提取时保留原实现方式——上面伪代码仅示意"改为可重复调用"；以现文件的标签实现为准，把一次性 forEach 搬进方法体，构造期与 `acquire` 后各调用一次。）

- [ ] **Step 3: 全停门 + 开卡流程**

frame() 的 run 块（锚点 `this._runAlpha = this.gameClock.advance(raw, this._runTick);`）改为：

```js
      const frozen = this.upgradeUi.isOpen;
      if (!frozen) {
        this._runAlpha = this.gameClock.advance(raw, this._runTick);
      }
      if (!frozen && this.run.active && this.run.pendingLevels > 0) {
        this.run.pendingLevels--;
        this.upgradeUi.open(this.upgradePool.draw(this.pickups.level), {
          rerolls: this.modifiers.passiveLevel('reroll'),
          summary: this._buildSummary()
        });
      }
```

同帧其余 run 渲染调用保持原位（冻结时仍渲染定格画面）。全停的另外三面：
1. `_steer` 调用处（锚点 `this._steer(raw);`）改 `if (!(this.runMode && this.upgradeUi.isOpen)) this._steer(raw);`
2. 帧首 dt 计算（锚点 `const dt = this.paused ? 0 : raw * settings.global.timeScale;`）改 `const dt = this.paused || (this.runMode && this.upgradeUi?.isOpen) ? 0 : raw * settings.global.timeScale;`（VFX/角色动画同冻）
3. `_handleAction` 顶部加 `if (this.runMode && this.upgradeUi.isOpen) return;`（键盘已被 UI capture 拦截，此为双保险，覆盖 HUD 点击路径）

`_buildSummary()`：

```js
  _buildSummary() {
    const parts = this.loadout.seats
      .map((element, seat) =>
        element ? `${RUN_SLOT_KEYS[seat]} ${element} Lv${this.loadout.levelOf(element)}` : null
      )
      .filter(Boolean);
    return parts.join('　');
  }
```

- [ ] **Step 4: 应用选择 + 弃权 + 刷新**

```js
  _onUpgradeChoice(result) {
    if (result.action === 'reroll') {
      this._rerollsLeft = (this._rerollsLeft ?? this.modifiers.passiveLevel('reroll')) - 1;
      this.upgradeUi.open(this.upgradePool.draw(this.pickups.level), {
        rerolls: Math.max(0, this._rerollsLeft),
        summary: this._buildSummary()
      });
      return;
    }
    this._rerollsLeft = null;
    if (result.action === 'skip') {
      this.playerState.hp = Math.min(
        this.playerState.maxHp,
        this.playerState.hp + this.playerState.maxHp * settings.upgrades.skipHeal
      );
      return;
    }
    const card = result.card;
    if (card.kind === 'upgrade') {
      this.loadout.upgrade(card.element);
      this.modifiers.bumpDamage(card.element);
    } else if (card.kind === 'new') {
      this.loadout.acquire(card.element);
      this._syncRunHudLabels();
    } else if (card.kind === 'passive') {
      this.modifiers.bumpPassive(card.passive);
      if (card.passive === 'vitality') {
        const grown = settings.run.playerHp * this.modifiers.maxHpMult();
        this.playerState.hp += grown - this.playerState.maxHp; // heal the delta
        this.playerState.maxHp = grown;
      }
    }
  }
```

- [ ] **Step 5: 冷却修正 + 移速修正 + 回响**

`_cast`（锚点 `this.cooldowns.set(element, Math.max(0, settings[element].cooldown));`）改：

```js
    const cdMult = this.runMode ? this.modifiers.cooldownMult() : 1;
    this.cooldowns.set(element, Math.max(0, settings[element].cooldown * cdMult));

    // 施法回响: a run-mode cast has a chance to fire itself once more.
    if (this.runMode && !this._echoing && this.runRng() < this.modifiers.echoChance()) {
      this._echoAt = { element, t: 0.15 };
    }
```

（constructor run 块把 rng 存为 `this.runRng = rng;`。）frame() run 块（未冻结分支内）加：

```js
      if (this._echoAt && (this._echoAt.t -= raw) <= 0) {
        const { element } = this._echoAt;
        this._echoAt = null;
        this._echoing = true;
        this.cooldowns.set(element, 0); // the echo is free
        this._quickCast(element);
        this._echoing = false;
      }
```

`_steer` 内 `this.character.move(this._moveDir, dt);` 改：

```js
    this.character.move(
      this._moveDir,
      dt,
      this.runMode ? this.modifiers.moveSpeedMult() : 1
    );
```

重开（锚点 `case 'restart':` 内 `this.run.start();` 前）加 `this.loadout.reset(); this.modifiers.reset(); this._syncRunHudLabels(); this._echoAt = null;`。`dispose()` 加 `this.upgradeUi?.dispose();`。

- [ ] **Step 6: 浏览器全流程验证**

`npm run dev` → `#run`（确认 `draft loadout` 开启）：
1. 开局只有 LMB 一个技能位亮着（loadout[0]），其余置灰
2. 杀怪升级 → **世界全停**（敌人/特效/走位/施法全部冻结，编辑器暂停语义不受影响）→ 三张卡弹出
3. `1/2/3` 或点击选卡：选"新技能"→ 下一个键位亮起立即可用；选"升级"→ 该技能伤害飘字变大（×1.25 可感）；选"疾行"→ 移速肉眼变快
4. `4`/弃权按钮 → HP 回 10%；拿过时来运转后弹卡界面出现"刷新"按钮且次数正确
5. Lv5 的那次三选一必含"新技能"卡
6. 凝神后技能冷却环转得更快；回响偶发同技能免费双发
7. 死亡 → Enter 重开：单技能开局、修正清零、标签复位
8. 去掉 `#run` 刷新：沙盒逐帧如常
同时 `npm run check` / `npm run check:game` 双绿。

- [ ] **Step 7: Commit**

```bash
git add src/core/App.js src/run/RunHud.js
git commit -m "Close the growth loop: freeze, choose, apply, echo"
```

---

### Task 8: 收尾（README + 勘误位 + 双校验）

**Files:**
- Modify: `README.md`（Run mode 段补：draft loadout、三选一键位、弃权/刷新）
- Modify: `docs/superpowers/plans/2026-08-15-m2-growth.md`（文末留"执行后勘误"空节标题，供执行会话回填）

- [ ] **Step 1: 双校验最终确认**

Run: `npm run check && npm run check:game`
Expected: 双绿；`npm run sim` 顺跑一遍确认难度带未被 M2 数值破坏（修正层期望值与 sim 的 +12%/升级假设同源）。

- [ ] **Step 2: README + 勘误空节 + Commit**

```bash
git add README.md docs/superpowers/plans/2026-08-15-m2-growth.md
git commit -m "Write the growth loop down and leave the errata slot open"
```

---

## Self-Review 结论（已执行）

- **Spec 覆盖（M2 范围）**：修正层→T1/T4；draft 席位→T2；卡池权重/里程碑/防重复→T3；升级数队列→T5；三选一 UI/弃权/时来运转→T6/T7；全停暂停→T7；回响/凝神/疾行/淬体/拾荒全部接到效果端→T1+T4+T7。自动施法（spec §6）**明确移入 M3**——它依赖"最近敌人"查询，与 M3 吐息者的索敌是同一块地基，一次做对。
- **占位符扫描**：Step 2 of Task 7 含一处"以现文件实现为准"的提取指引——保留，因为该 HUD 标签块的具体写法只有执行时的真实文件可依（M1 勘误教训：纸上锚点必须让位于真实代码）；除此无 TBD。
- **类型一致性**：`Card` 结构在 T3 定义、T6/T7 消费一致；`elementAt/equippedList/levelOf` 命名三处一致；`CombatSystem(targets, modifiers)` 与既有断言的单参调用兼容（默认值）；字段/方法无同名（勘误规程 ①：`seats` 字段无同名方法、`mods` 字段无同名方法）。

## 后续计划队列

M3 节奏（潮汐/精英/残章/结算 + 自动施法）→ M4 五行深度（印记/相克/融合/共鸣）→ M5 包装（禁咒/音效/正式 HUD 血蓝瓶/场景光影/双语）→ M6 量产（13 新技能模板化 + 平衡通调）

### 执行后勘误（执行会话回填此节）
