# 全任务复核报告(M1-M12)

> 2026-08-18,按用户指令执行。方法:①十二份计划的勘误节逐条对账(每个未收项判定 已收/本次修复/接受/待裁定/待立项);②硬性不变量横向扫描;③全量回归。**本报告是账目的汇总处;各里程碑勘误节仍是偏差的原始记录。**

## 一、回归状态(复核当日)

| 门 | 结果 |
|---|---|
| `npm run check:game` | **绿**(游戏逻辑无头断言全量) |
| `npm run check` | **绿**(角色/剪辑装载) |
| `npm run build` | **绿** |
| `npm run sim` | **四锚全中**:新手 0% / 基线死亡中位 13.0min / 熟练 100% / 等级中位 37;基线胜率 34% |
| `npm run report:bp` | 30 技 × 3 档全量出数,console error 0(常驻光环按落座测,M12 T4) |

## 二、不变量横向扫描

| 不变量 | 扫描方法 | 结论 |
|---|---|---|
| **frustumCulled = false** | 全库 13 处 `new InstancedMesh`(archive 除外)逐文件比对 | **全部达标**,每个创建文件都有对应设置 |
| **双语 strings** | zh/en 键集合对比 | **118 = 118,零缺口** |
| **数值分层**(代码不写 settings) | 全库赋值扫描 | 命中 5 处,**全部合法**:PauseMenu 三处是用户偏好开关(language/reduceFlashes/performanceMode)、CameraRig 的 fixedYaw 是取景状态、App 的 roamRadius=arenaRadius 是文档化的 run 模式配对。战斗数值零违例 |
| **零分配热路径** | 新增系统(Boss/Altar/HUD)per-tick 路径人工核 | **一处违例,本次修复(F1)**:`BossSystem.tick` 每 tick 分配 `here` 字面量——M11 自己立的规矩自己犯。改为模块级 scratch(与 `_at` 的 `_p` 分开,后者会被招式调用中途覆写) |
| **五字段施放不变量 / 沙盒纯净 / 771ba02 scratch** | 既有断言套件覆盖 | 套件绿即达标 |
| **镜像规则**(M11 立) | sim 全参数已接 settings 并有扰动断言;bp-report 无副本 | 达标 |

## 三、本次复核的两个修复

- **F1 · BossSystem 热路径分配**(上表)。
- **F2 · 弑神一闪预瞄终于按缩放射程画**——M6 T12 的 T-later 项,开了五个里程碑:Lv3 冲 10.4m,箭头只画 8m(预览低估 30%)。当年搁置的原因是"controller 没有 loadout 访问权限";M10 T3 注入 `levelOf` 之后,修复只剩一个 getter(`rangeScaled`),三处钳制换读点。断言钉住 Lv3 = `scaledDashRange(3)`、Lv1 = 基值恒等;破坏验证红。

## 四、逐里程碑账目对账

**✅ = 已收(注明在哪收的) 🔧 = 本次修复 🤝 = 接受的设计取舍 ⚖️ = 待用户裁定 📋 = 待立项(有真实工作量)**

### M5
- ✅ castEarth 音效休眠 → M6 土系落地 + T7 断言收账。
- ✅ preload 门控 → gate 脚本注入(index.html 复核确认)。

### M6
- ✅ quake 击退死代码 → T11 前收(bd7aa5a)。
- ✅ shield-gate watch → T12 CLOSED(裁定 ACCEPT)。
- 🔧 **T-later:弑神一闪预瞄未缩放 → 本次 F2 修复**。

### M7
- ✅ ctx.rng 三处确定性回退 → M8 接线(`App.js:356 ctx.rng = runRng`),类内回退保留为沙盒 null-safe,正确。
- ⚖️ **母系金是否吃淬炼**(现:只看子系)——机制级,须用户裁定;若改须回流 spec §4.8。
- ⚖️ **点打技挤堆多付**(落雷/链电对相拥敌群 ×1.83)——是否 damageOnce 化,平衡裁定。
- 🤝 chainbolt 数值锚按数值表裁(3 击/道),已记录。

### M8
- 📋 **`damage()` 基线冲量对 zoneTick/lineTick 逐 tick 施加**(snare 峰值 28.7 m/s)——M8 之前的老账,动手感,**建议单独立项**(全库 kbScale 语义已就绪,工作量在重调各技手感)。
- ✅ 十技无质变表 → M10 T1/T2 收。
- ✅ 扇形用直线指示器 → M10 T3 收。
- 🤝 CRACK 贴花满尺寸/满 alpha 窗 45-55% 重叠——GLSL 归一化性质,改 shader 不值当。
- 🤝 雷暴 `age` 的 fade 项对当前数值是死代码(闸必要,雷全落 impact 窗)——复核确认仍如此,保留。

### M9
- ⚖️ **异化是通用四条**(疾发/沉重/回响/超载),非每技定制——spec 只给了名字。
- 🤝 满座全满级全异化 → 空手牌 → skip-heal。
- 📋 `wide`/`pierce` 类异化的前置(形状读取集中化 + 伤害路径带技能 id)——独立工作量。
- 🤝 无尽难度线性外推,跑够久必死——设计取舍。
- 🤝 `roll <= 0` 兜底的极端权重行为——与 `_take` 同宗,已有多样性断言看门。

### M10
- ⚖️ **二十档质变数值**(新裁,25-35%/档)——`docs/breakpoint-review.md` 有全部实测,待用户按表复核。
- 🤝 burst 伤害档从 M6 起是死的、修通后 v1 部分 burst 技变强——已记录,未回调数值。
- ✅ 扇形指示器单技服务 → 断言钉了 CONE⟺coneTick,第二个扇形技零成本。
- 🤝 扇形 valid 红染走 zone 老路(小瑕)。
- 🤝 quickCast 对扇形按直线取满射程(语义冗余无害)。

### M11
- ⚖️ **Boss 数值对 sim 调的**(hpMult 260 / 冲锋 14 / 震地 11),`bossOverlap` 0.6、`bossTelegraphed` 0.35 是建模判断——**待实机手感复核**。
- ✅ 首领奖励被拾取上限吞 → M12 T1 收(0/14 → 14/14)。
- ✅ 报告光环盲区 → M12 T4 收。
- ✅ spec §12 程序化 Boss 回流 → 已写入 spec;替换点 `BossRenderer`。

### M12
- ⚖️ 祭坛奖励 = 定向手牌(非临时增伤)。
- ⚖️ 本命配对 sorcerer→疾行 / classic→活力。
- 🤝 纯护盾量在报告量程外(脚注已改口)。
- ⚖️ 明确不做并等点名:第三波十技 / BGM / 每日种子+分享卡。
- 🤝 祭坛无专属音效(打磨项)。

## 五、待用户裁定汇总(全部 ⚖️ 一处看完)

1. **母系金淬炼**(M7):现只看子系;要不要让母系金融合也吃淬炼?
2. **点打技挤堆多付**(M7):落雷/链电对挤堆 ×1.83,要不要 damageOnce 化?
3. **异化通用四条**(M9):要不要每技定制?
4. **二十档质变数值**(M10):对着 `docs/breakpoint-review.md` 的实测表过一遍。
5. **Boss 手感**(M11):hpMult 260 / 招式伤害 14/11 是模型定的,实机打一场。
6. **祭坛奖励形态**(M12):定向手牌 vs 临时元素增伤。
7. **本命配对**(M12):sorcerer→疾行 / classic→活力。
8. **下一步内容**(M12):第三波十技 / BGM / 每日种子+分享卡,做哪个、先做哪个。

## 六、待立项汇总(📋,有真实工作量)

1. **zoneTick/lineTick 的逐 tick 基线冲量**(M8 老账):语义机器已就绪,工作量在逐技重调手感与实测。
2. **wide/pierce 类异化的前置改造**(M9):形状读取集中化 + 伤害路径带技能 id。

## 七、复核结论

十二个里程碑的勘误账目**全部有下落**:已收 9 项、本次修复 2 项、接受 10 项、待用户裁定 8 项、待立项 2 项——**零失踪账目**。六项硬性不变量横向扫描一处违例(本次修复)。三套件、sim、报告全量绿。

复盘一条贯穿性的观察:这个项目最值钱的资产是**四条测试规则的演进链**(M8 冻结规则 → M9 镜像式断言 → M10 注入规则 → M11 扰动规则)——十二个里程碑里几乎每一个"全绿交付的缺陷"都落在这四类里,而每一条规则立起来之后,下一个里程碑就真的用它抓到了东西(M12 的 `character.id` 缺失、M11 的 Boss 挤不进场、复核自己的 F1/F2)。规则在 CLAUDE.md,证据在各勘误节。
